name: ci
on:
  push: { branches: [main] }
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm test
      # Dev build: proves the seed is valid, HSDS-valid, and signable. Release builds run
      # separately with BUNDLE_SIGNING_KEY and fail until emergency numbers have been phoned.
      - run: pnpm build:bundle
