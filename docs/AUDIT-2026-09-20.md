# Audit, 2026-09-20: what is active, and what is only planned

> **Since this audit (same day, later).** The body below is left exactly as it was written, because an audit that
> gets edited afterwards stops being evidence. What has changed since:
>
> - **418 listings → 528**, and 44 of the 46 categories are filled. Of the "seven categories with no listings",
>   only warming and cooling centers are still empty, which is correct: they are alert-driven. Health Department
>   programs (24), safer-use supplies, rides, walk-in crisis and recovery support all have live rows.
> - **Two new categories**, `health.er` and `health.urgent`.
> - **361 tests → 461** (query 111, api 80, pipeline 161, web 109), plus 111 Swift fixture cases and 28 new iOS
>   app tests.
> - **Five tabs → four**: Home · Help · Map · Events. Recreation and Transit merged, with 11 transport layers in
>   the signed bundle. That also means the "Events tab as dead code" note now describes a tab that hides itself.
> - **The six deploy blockers**: Kyle created the D1 database and committed its id, created the Access
>   application, created the WAF rate-limiting rule, and signed for both in `api/edge-protections.md`. The gap this
>   audit named — "preflight cannot see the two things it depends on most" — is closed by that attestation plus
>   `pnpm smoke`, which proves both from outside a live origin. **Rate limiting is no longer only a comment.**
> - **The 56 unfinished hand-checks are worked**, including the 38 a bot challenge had blocked, which Kyle cleared
>   himself in his own Chrome. What is left for a person is in [CHECKS-2026-09-20.md](CHECKS-2026-09-20.md).
> - **The iPhone app** gained reports, saved places and an About/privacy screen. **An Android client now exists in
>   Kotlin and has never been compiled.**
> - **A full WCAG 2.2 AA pass** over the web app: 21 failures fixed, 4 open
>   ([ACCESSIBILITY-AUDIT-2026-09-20.md](ACCESSIBILITY-AUDIT-2026-09-20.md)).
> - **The stale DECISIONS row** this audit names (the nightly re-check "not built") is now marked Superseded and
>   points at its correction.
>
> **Later the same evening**, which is after everything above:
>
> - **Arabic and Bengali** were drafted, so the app has four languages, Arabic mirrored right to left, on web,
>   iPhone and Android. **Machine-drafted; no native speaker has read either one**, and Spanish is still waiting
>   for a reviewer too.
> - **The Android client was compiled.** Kyle OK'd the Android SDK; `:app` compiled, its tests ran, it assembled a
>   1.13 MiB debug APK and it **ran on an AOSP API 35 emulator** — never on a phone, and nothing below Android 15
>   has run it. Five real errors surfaced, two of which would have crashed every phone from `minSdk` 24 to
>   Android 14. The CI job was fixed in the same pass and now builds `:query` and `:core` only, on purpose
>   (`HELP313_NO_ANDROID=1`).
> - **A "Bus directions in the Transit app" link-out** on web, iPhone and Android: destination only, never an
>   origin or identifier, absent where directions are withheld.
> - **Numbers, re-derived from a build on 2026-09-20:** **531 listings** (the figures above say 528; 531 is right),
>   44 of 46 categories filled, and `pnpm test` at **503** (query 111, api 80, pipeline 179, web 133).
> - **A root `README.md`**, which this repository did not have while this audit ran.
>
> Still true, and unchanged: **nothing is deployed, and nothing scheduled has ever run.** The domain in
> `ALLOWED_ORIGIN` does not resolve, and `pnpm preflight` still stops on the three key checks.

Kyle asked what is really working versus what has been written down but not built. Everything below was checked in the code, in the data files, or by running something — not by trusting a doc. Four detailed reports sit in [audit-2026-09-20/](audit-2026-09-20/): [web](audit-2026-09-20/web.md), [pipeline and data](audit-2026-09-20/pipeline-and-data.md), [API and operations](audit-2026-09-20/api-and-operations.md), [iPhone and backlog](audit-2026-09-20/iphone-and-backlog.md).

## The short answer

**The app is real and the data is real. Nothing is deployed, and nothing that is supposed to run on a schedule has ever run.**

| | State |
|---|---|
| Web app | **Active.** Every tab, all 23 need screens, refinements, link-outs, search, ZIP entry, saved places, add-a-place, reports with an offline queue, the drawn map, 205 neighborhood pages, freshness badges, Spanish. Builds at 66.9 KB gzipped. |
| Data | **Active.** 418 listings in a signed bundle (358 researched rows plus 60 Health Department Narcan stations), valid HSDS on every build, 37 of 44 categories filled. |
| Pipeline | **Active for building; dormant for watching.** Import, entry check, geocode, ingest, indicators and bundle-signing all work. The nightly re-check, steward tasks and report sync have never run against anything. |
| Worker, D1, admin page | **Built, tested, never deployed.** The whole loop runs locally: report → steward queue → archive → back into the bundle. |
| iPhone app | **Compiles and runs.** Reads the signed bundle offline. Missing reports, saved places, the map, neighborhoods, transit, About, and 8 release blockers. |
| Deployment | **Nothing exists.** No domain, no Cloudflare resources, no release signing keys, no store accounts. *[Corrected 2026-09-20, later the same day: a Cloudflare account, a real D1 database (its id committed), a Worker project building from this repository, the Access application and the WAF rate-limiting rule all exist — see the note at the top of this file. Still true: no domain, no release signing keys, no store accounts.]* |

## What runs today, proven

- `pnpm test`: **361 tests, all passing.** query 111, api 68, pipeline 113, web 69. (While this audit ran, one web test failed because the working copy had lost the publish workflow; see the correction below.)
- `pnpm build:bundle` produces a current, signed bundle (dev key): 418 listings, 52 greenway segments, 302 parks, 205 neighborhoods, 29 files, all checksummed.
- The Worker was run locally against its four migrations and driven end to end: a report was deduplicated per phone per day, reached the steward queue, was archived, and the counts flowed back. Its zero-PII rules held under live testing: an unknown field is rejected, free text is masked, condition reports keep only the hour, and no IP or identifier reaches the database.
- The iPhone app builds and runs in the simulator, verifies the signed bundle offline, and its shared rules library passes all 111 fixture cases.

## What is written down but not built

**Nothing runs on a schedule.** The nightly publish, the page re-check that raises steward tasks, the 180-day report purge and the 30-day photo delete are all code that has never executed. They need a deployed Worker and a workflow runner.

**Corrected after the audit: the repository was fine; this working copy was not.** While auditing, the folder had no `.git` and no `.github/`, so the publish workflow and CI that five documents describe appeared to be missing and one web test failed looking for the workflow file. The repository on GitHub had them all along. The history and the missing files (`.gitattributes`, `.gitignore`, `ci.yml`, `publish.yml`) were restored into this copy on 2026-09-20, the whole suite went green, and everything in this audit was pushed and merged ([pull request 1](https://github.com/kp2485/313-help/pull/1)). What remains true: **CI and the nightly publish have still never done any publishing** — the publish job runs on schedule and skips, because `PUBLISH_ENABLED` is unset and there is nowhere to deploy to.

**Six things block a deploy** (`pnpm preflight`): two pinned public keys, a release signing key, a real D1 database id, the live site's origin, and the Cloudflare Access settings. Preflight cannot see two things it depends on most — the Access policy and the rate-limit rule — so a checkout could pass every check and still deploy an unprotected API.

**Rate limiting exists only as a comment.** By decision it lives in a Cloudflare rule rather than code, and that rule does not exist yet. Today the only abuse defense is one report per phone per listing per day. *[Corrected 2026-09-20, later the same day: Kyle created the WAF rate-limiting rule and signed for it in `api/edge-protections.md`; `pnpm preflight` blocks a deploy until that line is signed, and `pnpm smoke` proves the rule from outside a live origin. See the note at the top of this file.]*

**Three screens are code-only because their data is empty:** alerts (0), archived listings (0, so "closed places" can never appear in search), and City events, which were dropped and left the Events tab as dead code.

**Seven categories have no listings:** warming and cooling centers (alert-driven, expected), safer-use supplies, Health Department programs, rides, walk-in treatment and recovery support.

**Photos and the sunset banner are built and switched off**, which is correct but means neither has been exercised.

## Two bugs found and fixed during the audit

1. **The About screen would have printed a raw key.** Its paragraphs are built in a numbered loop, and removing the "not an official City app" line yesterday left a hole, so the screen would have shown the literal text `about.p3`. Paragraphs renumbered; a test now ties the loop to the strings.
2. **The tab bar reserved five columns for four tabs**, leaving an empty slot and shifting the tabs left on every screen, since Events hid itself in TypeScript only. The bar now sizes itself to however many tabs show; a test guards it.

## Docs that no longer match the code

Stale in the bad direction: a DECISIONS row still says the nightly re-check and steward tasks are "not built" (both exist and pass tests); docs/06 says the app shows a "reporting available" indicator from `/v1/health` (it never calls it); docs/05 still says four shortcuts on Home (there are six) and shows five tabs; docs/03 lists bundle files that are never produced; docs/04 says the urgent-numbers file has six rows (it has eleven).

Stale in the good direction: food-poor neighborhoods are 29 of 205, not 93; listings are 418, not 178; shower listings 4, not 2; listings needing a by-eye check 24, not 33; and the install-key reset button that docs/08 calls unbuilt is live.

Also worth knowing: the committed greenway access report is from an older bundle (49 listings, 18 segments flagged) and understates today's coverage (114 listings, 16 flagged, 3 segments with nothing nearby), and `help.coverage_checked` on neighborhood pages can never be true, because the build never passes that flag.

## The backlog, by who it waits on

| Waiting on | Count | The big ones |
|---|---|---|
| **Kyle** | 27 | The deploy chain (domain, Cloudflare, keys, about $10 a year plus usage); store developer accounts; the call to DWIHN about uninsured treatment; Wayne County's Narcan map; SEMCOG crash data terms; legal advice before photos; a review of the overdose steps; a Spanish speaker to check the translation |
| **A steward, by hand** | 60 | All 56 unfinished rows of `CHECKS-2026-09-19.md` — the shelter line, 33 listings on sites that block scripts, the 18 Health Department programs, 4 held listings — plus 4 judgement calls. None has been filled in |
| **Just work** | 61 | 26 iPhone items (reports, saved places, map, neighborhoods, transit, About, release blockers), 6 open review items, 28 "later / v1.1 / v2" doc items |

DECISIONS has 160 rows: 12 are still open (9 Open, 2 Proposed, 1 Deferred). Of the 34 items in the September 19 review, 24 are verifiably fixed in code; the rest are listed in the detailed report with evidence.

## What I would do next, in order

1. ~~Put this checkout back under git~~ **Done 2026-09-20**: the history and workflows were restored and everything here was merged into `main`. CI runs on every push and pull request again.
2. **Deploy** (Kyle's accounts): keys, D1, Worker, Pages, Access, then the WAF rate-limit rule. That alone turns six built-but-dormant systems on.
3. **Work the hand-check list.** 56 rows, and 288 held listings, are the difference between a good directory and a better one.
4. **Fill the empty categories** that are not alert-driven: safer-use supplies, rides, the Health Department's programs.
5. **iPhone**: reports, saved places and the About/privacy screen, then the release blockers.
