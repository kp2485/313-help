# DECISIONS

Decisions the numbered docs don't cover, or where they were changed. One line of rationale each. **Proposed** = recommended by the audit (doc 10) or the places proposal (doc 11), waiting on Kyle. **Accepted** = in force; update the numbered doc to match. **Open** = a known gap: decided or designed, not built. **Superseded** = replaced by a later row, which it names.

| Date | Status | Decision | Rationale |
|---|---|---|---|
| 2026-09-18 | Accepted | Design docs live in `docs/`; CLAUDE.md stays at repo root. | CLAUDE.md and 06 already assumed this layout. |
| 2026-09-18 | Accepted | Public repo carries roles, not names, for DHD staff and partner contacts. | Public repo; people didn't agree to be named. |
| 2026-09-18 | Accepted | **No institution maintains data for us.** There is no DHD spreadsheet or feed and no authorization letter; none is assumed. Stewards maintain `data/seed/`; page watchers on public DHD pages open steward tasks and never auto-publish; `owner_feed`/`owner_attest` remain in the schema for orgs that opt in later. Docs 01–04, 06–09, README, CLAUDE.md updated. | Kyle, 2026-09-18: DHD did not respond as though it would maintain anything. |
| 2026-09-18 | Accepted | Harm-reduction station cadence 14 → 30 days, verified by phoning the host site; stock-outs carried as same-day signals. | 14 days assumed an owner. An honest 30 beats a fictional 14. |
| 2026-09-18 | Accepted | **No scheduled verification.** Humans act on exceptions only: check at entry, weekly pass over reported rows and machine-raised tasks, monthly calls to ~15 safety-critical rows (calls later replaced by a machine match against the owner's page, `check:emergency`; see "Use the currently published numbers"). Badges state facts and never say "verified" for an unchecked row. (04, 10-A6) | Open-source gift with no paid operator; a standing calling job would not be staffed. |
| 2026-09-18 | Accepted | The app is open source and intended as a gift to the city. It must run unattended, degrade honestly, shut itself off safely, and be transferable. (doc 12) | Kyle, 2026-09-18. |
| 2026-09-18 | Accepted | Community reports never hide a row; they demote and label. Only a steward changes visibility. (10-A1) | Client nonces are forgeable, so "two independent reports" is two HTTP requests. |
| 2026-09-18 | Accepted | Presence in a source (open-data layer, watched page, any future feed) is never verification and never resets the clock. (10-A2) | "Still listed" only means the publisher hasn't removed it. |
| 2026-09-18 | Accepted | Bundle ships verification facts; clients compute stale/badge/sort from the device date. Bundle-age banner after 72h. (10-A3) | Build-time badges lie on offline phones. |
| 2026-09-18 | Accepted | `client_nonce = sha256(install_secret ‖ target_id ‖ day)`; reword the non-negotiable to "no identifier ever leaves the device." (10-A4) | Daily nonce links one person's reports into a location trail; on-device UUID contradicted CLAUDE.md as written. |
| 2026-09-18 | Accepted | 911 and 988 hardcoded, never overridable. Any phone/address/coordinate change on any row is held for steward approval. Bundles are Ed25519-signed; clients pin the public key. (10-A5) | Closes the sheet → pipeline → emergency strip hijack path. |
| 2026-09-18 | Superseded (by "Use the currently published numbers") | Shelter button uses 866-313-2520 until both numbers are verified by phone. Emergency CSV carries `verified_by_call_on`; build fails if older than 30 days. (10-A9) | City now directs shelter seekers to the Housing Resource HelpLine. |
| 2026-09-18 | Accepted | Split the overdose triage entry into "overdosing right now" (911 + steps only) and "I want Narcan to carry." (10-A7) | Two different users; a map is harmful to the first. |
| 2026-09-18 | Accepted (built in `packages/query/src/freshness.ts`: tiers are sort keys only, the method is part of the badge text) | Badge = f(state, age ÷ cadence, latest verification method). Numeric score is a sort key only. (10-B1) | Current formula can never give a phone-verified community pantry the good badge. |
| 2026-09-18 | Accepted | One ranking rule: eligibility → distance band → open-now/next-open → freshness → distance. (10-B2) | Docs had three; confidence-before-distance fails people without cars. |
| 2026-09-18 | Proposed | `signals.json` on R2, rewritten ~every 15 min, for same-day signals with TTLs. (10-B4) | "No food today" can't wait for a nightly build. |
| 2026-09-18 | Superseded (see "The app draws its own street map" below) | Self-hosted PMTiles basemap + MapLibre instead of Leaflet + OSM tile servers. (10-B8) | OSM policy forbids offline prefetch; third-party tiles leak where users look. |
| 2026-09-18 | Superseded (by the "`rrule` stays… floating wall-clock" row) | Evaluate `rrule-temporal` against `rrule` on the DST fixtures; pick the one that passes. (10-B3) | `rrule` has open TZID/DST bugs. CLAUDE.md names `rrule`, so this needs Kyle's OK. |
| 2026-09-18 | Accepted (in `api/wrangler.toml`; OPERATIONS step 7) | Worker: `invocation_logs = false`, Logpush off, no header logging; edge rate limiting is the only IP-keyed control and we never read the IP. | Default Workers Logs capture request metadata. |
| 2026-09-18 | Accepted (alerts are written by a person with `pnpm alert:new`; no press-release parser or auto-publish exists) | Press-release alerts always need a human publish. No auto-publish, ever. (10-B10) | Offline phones can't receive a retraction. |
| 2026-09-18 | Proposed | Enroll Apple/Play developer accounts as an organization (Linwood Technologies). (10-B5) | App Review 5.1.1(ix). |
| 2026-09-18 | Accepted | Scope widens from "help for people in need" to "help and healthy places." See doc 11 for the guardrails. | Greenway/parks integration; changes 01 "What it is NOT." |
| 2026-09-18 | Accepted | New ID prefixes `plc_` (place), `seg_` (greenway segment), `cond_` (condition report). | CLAUDE.md lists the allowed prefixes; places need their own. |
| 2026-09-18 | Accepted | Condition-report photos are not part of the dataset: private bucket, steward-only, deleted 30 days after the report closes. | "Nothing is deleted" protects resource history; it must not become a photo archive of neighborhoods. |
| 2026-09-18 | Superseded (by "Five tabs" and "Urgent help" rows) | Needs list sits directly on Home; location is never a gate; emergency strip is two fixed buttons + "More urgent numbers." (10-B13) | The main path took five taps, breaking Principle 3. |
| 2026-09-18 | Accepted | Open-data rows are candidates in `data/staging/`, not auto-published; they get the same entry check as any row. Layer last-edit date is recorded; staff contact fields are never ingested. | The City's Recreation Centers layer was last edited Nov 2016. |
| 2026-09-18 | Accepted | Shared query code lives in `packages/query` (not in 06's layout). | Pipeline tests and the PWA must run the same open-now/badge/ranking code. |
| 2026-09-18 | Accepted | `rrule` stays (per CLAUDE.md) but only in floating wall-clock mode: it generates calendar dates, and open/close comparison happens in America/Detroit wall time via `Intl`. No TZID features used. `rrule-temporal` not adopted. | Avoids rrule's known TZID/DST bugs without a Temporal polyfill on cheap phones; door signs are wall-clock anyway. |
| 2026-09-18 | Accepted | HSDS 3.2 compiled schemas are fetched into a git-ignored cache for validation, not vendored. | Upstream license is not a standard permissive one; CLAUDE.md says ask before adding such material. |
| 2026-09-18 | Accepted (refines the open-data row above) | An open-data layer its publisher edited within 90 days may publish straight to the bundle (`mode: publish` in `sources.yaml`). Rows get the `source_listed` badge — "From the {list}, last updated {date}" — which names the list and claims nothing else; past 90 days it becomes `never_checked`. Older layers still stage. Approved by Kyle (see "Kyle OK'd publishing the DHD station layer"). | DHD's harm-reduction layer was edited 2026-08-26 and has 60 in-bbox stations; hand-checking each before the hackathon isn't possible, and the badge tells the truth. |
| 2026-09-18 | Accepted | New entry method `auto_check`: `pnpm check:sources` fetches a row's `source_url` and promotes it from `proposed` only if its phone number and street number appear in the raw page text. Badge: "Matched their website when added." Rows whose site blocks fetching stay `proposed` for a person. | Seed research read pages through a summarizer; a raw-text match is a real, repeatable check, and doubles as the nightly auto-check. |
| 2026-09-18 | Accepted | Hours text from a list is shown as written (`hours_text`) and never parsed into a schedule; only an unambiguous "24 hours" becomes open-now. | "Varies by day" and "9-10 M-S" can't be turned into open-now without guessing. Unknown is never open. |
| 2026-09-18 | Accepted | HSDS export: `id` is a deterministic UUIDv5 of our slug (HSDS requires UUIDs); the slug rides in `x_detroit.id`. HSDS `timezone` (a numeric UTC offset) is omitted because it is wrong for half the year; times are wall-clock with `x_detroit.timezone: America/Detroit`. DAILY schedules export as WEEKLY on all seven days (HSDS allows only WEEKLY/MONTHLY). | Found by validating against the HSDS 3.2 compiled schema. |
| 2026-09-18 | Accepted | Ingested and staged CSVs are committed; a phone/address change from a source appears as a git diff and is approved by merging it. | Implements the "held for steward approval" rule (10-A5) before an admin tool exists. |
| 2026-09-18 | Accepted | `heartbeat` = newest date a person phoned an emergency number or checked a row in, read from committed CSVs. | A nightly rebuild can't advance it (docs/12). |
| 2026-09-18 | Accepted | **Use the currently published numbers.** Emergency and listing phone numbers are whatever the responsible owner publishes today; the gate is a machine match against the source page within 30 days (`check:emergency`, `check:sources`), not a phone call. Shelter intake follows the City (866-313-2520). Supersedes the "phoned within 30 days" wording of 10-A9. | Kyle, 2026-09-18. Consistent with no scheduled calling. |
| 2026-09-18 | Accepted | Doc 11 (greenway, parks, condition reports) approved in full, including "things, never people." JLG segment ingest enabled. | Kyle, 2026-09-18. |
| 2026-09-18 | Accepted | Public-data indicators cover all 205 City neighborhoods, not only the greenway corridor; the greenway study area is one lens (doc 13). No neighborhood rankings, no per-neighborhood crime panel, counts under 5 suppressed, help-access numbers labeled as directory coverage. | Kyle, 2026-09-18, plus the finding that nine open segments show zero listings with a 91-row directory. |
| 2026-09-18 | Accepted | Rows whose own site blocks scripted fetches may be entry-checked by reading the live page in a browser (`entry_method: web`). | Wayne Metro and YWCA return 403 to scripts; both were confirmed in a browser. |
| 2026-09-18 | Accepted | Code license Apache-2.0 (`LICENSE`, `NOTICE`); dataset CC BY 4.0. | Kyle, 2026-09-18. |
| 2026-09-18 | Accepted | Kyle OK'd publishing the DHD station layer with the `source_listed` badge and no per-station entry check. | Kyle, 2026-09-18. |
| 2026-09-18 | Accepted | PWA is Vite + plain TypeScript, no UI framework; `@noble/ed25519` (MIT) verifies the bundle because older Android browsers lack WebCrypto Ed25519. App JS was 29 KB gzipped at step 2 (about 57 KB on 2026-09-19, with the map, Spanish and neighborhood pages). | Primary user has a cheap phone and little data. 06 allowed "SvelteKit or vanilla." |
| 2026-09-18 | Accepted | Shareable screens use hash URLs (`#/r/sal_…`, `#/c/food`, `#/greenway/seg_…`). Home, the urgent-numbers sheet, every "need" screen, and DV/mental-health listings never change the URL. Quick exit uses `location.replace()` to a weather site. | Static hosting needs no rewrites; sensitive screens leave nothing in browser history (10-A8). |
| 2026-09-18 | Accepted | The service worker caches the app shell only and never `/data/`. Listing data lives in IndexedDB after signature and checksum verification; an older bundle than the one held is refused. | An unverified or replayed copy must never be served. |
| 2026-09-18 | Superseded (by "The app draws its own street map") | The PWA draws the greenway as inline SVG from bundled geometry; no map tiles in step 2. | Works offline, zero third-party requests; the PMTiles basemap (10-B8) can come later. |
| 2026-09-18 | Superseded (all built: search, ZIP, Saved, help paying for food, reporting; the overdose-review item moved to its own Open row) | Not built yet in the PWA: ZIP-code entry as a location fallback, "help paying for food" link-outs, Saved, search, reporting UI (waits on the Worker). Overdose steps follow national guidance and still need DHD/MDHHS review before a public release (docs/08). | Scope for step 2 was Home, lists, detail, triage, offline. |
| 2026-09-18 | Accepted | Rate limiting is a Cloudflare WAF rule, not the Workers rate-limit binding. | The binding needs the Worker to pass a key, which would mean reading the IP in our code. With a WAF rule the Worker never touches an IP, and a test keeps it so. Replaces the wording in 10-A1.2. |
| 2026-09-18 | Accepted | API request bodies are closed schemas: unknown fields return 400. Repeat reports (same device, target, kind, day) are ignored but still answer 202. | A client bug that tries to send an id or coordinates fails loudly in development; a repeat looks the same as a first report from outside. |
| 2026-09-18 | Accepted | The Worker verifies the Cloudflare Access JWT itself (RS256, audience, issuer, expiry) and fails closed when Access isn't configured. The pipeline uses an Access service token. | Defense in depth: a misrouted or direct request can't skip the login. |
| 2026-09-18 | Accepted | Circuit breaker: closure reports on more than 5 distinct listings in 24h make the aggregates endpoint say so, and the bundle build ignores closure counts until a steward looks. | Mass closure is an attack or a disaster; both need a person (10-A1.4). |
| 2026-09-18 | Accepted | API tests run the real migration on Node's built-in SQLite behind a small D1-shaped wrapper. | Real SQL without the Workers test pool, which is heavy and fragile on Windows. |
| 2026-09-18 | Open | Still not built from step 3: Turnstile or app attestation, `signals.json` for same-day signals. (Photos, the add-a-place form and the nightly publish workflow were built later; the workflow stays off until `PUBLISH_ENABLED` is set.) | Step 3 scope was reports, proposals, steward endpoints. |
| 2026-09-18 | Accepted | **Five tabs: Home · Help · Recreation · Transit · Events.** The needs list moves from Home to the Help tab; the greenway moves to Recreation. Supersedes 05's four-tab bar and the B13 "needs on Home" layout. Path to a phone number is still three taps: Help → need → Call (or Urgent help → number: two). | Kyle, 2026-09-18: the app now serves everyday use as well as crisis, and should look like it. |
| 2026-09-18 | Accepted | The always-visible red emergency strip is replaced by an **Urgent help** button in the top bar of every screen. On DV and crisis screens that slot holds quick-exit, and the numbers are already first on the page. Red is used only for the 911 row and quick exit. Urgent needs lead the Help tab by order and wording, not color. | Kyle disliked the red strip and red overdose row. Emergency numbers stay one tap from anywhere (Principle 3); 911 and 988 stay hardcoded. |
| 2026-09-18 | Accepted | Design system: green palette with light and dark themes, system fonts, rem units, 44px+ targets, body text at 7:1 contrast, meaning never carried by color alone, reduced-motion and forced-colors support, hand-drawn inline SVG icons (no icon library). | "Modern and accessible" for a very wide audience on cheap phones. |
| 2026-09-18 | Accepted | City events come through the pipeline into the signed bundle; the app never calls detroitmi.gov. Facts only (title, date, time, department, link), no descriptive text. | No feed exists; a phone fetching the City's site would leak the user's IP to a third party and wouldn't work offline. |
| 2026-09-18 | Accepted | Transit is link-out plus a "Bus directions" button (Google Maps transit URL). No trip planning inside the app yet. | Planning a trip needs the rider's origin; handing that to a maps app the person already uses beats sending it anywhere ourselves. GTFS nearest-stop is the next step and can run on-device. |
| 2026-09-18 | Superseded 2026-09-19 | The app's first name, chosen by Kyle. Replaced by "313 Help" on 2026-09-19 (below) because a local organization's name was too close. | Kyle. |
| 2026-09-18 | Accepted | Docs corrected: D Compassion did ship (vendor-published "Detroit Compassion App," both stores, 2025). Public claims about it are limited to what its store listings show. The public repo should not be named after it; rename before it goes public. | Found while checking the name. A judge can look the app up. |
| 2026-09-18 | Accepted | GitHub repo renamed away from DHD's product name before going public (GitHub redirects old URLs; now `313-help`). The local folder name is unchanged. | "D Compassion" is a live DHD product; the repo is going public. Resolves audit C5. |
| 2026-09-18 | Accepted | Steward decisions about a listing (archive, pause, restore) are rows in D1 (`listing_overrides`), applied by the pipeline at build time. The API never edits seed or ingested files. Archiving needs one of the docs/04 reasons and settles the open closure reports; restoring clears them. A steward's decision is not held back by the circuit breaker. | Keeps git as the source for listings and D1 as the source for decisions; every decision is logged with who and why; nothing is deleted. |
| 2026-09-18 | Accepted | The admin page is plain HTML, CSS and JavaScript in `admin/`, served at `/admin/` on the app's origin, behind the same Cloudflare Access application as `/v1/steward/*`. No framework, no build step, no tokens in the page. | Docs/06: "boring on purpose." Someone else can maintain it. |
| 2026-09-18 | Superseded (by the 2026-09-20 row "The local steward login stops trusting the `Host` header") | Local-only steward login: `DEV_STEWARD` in `api/.dev.vars`, honored only when the request host is `localhost` or `127.0.0.1`. Tested against look-alike hosts. | The Worker fails closed without Access, which made local demos impossible. |
| 2026-09-18 | Accepted | Bundle version = git sha + minute + an 8-character hash of the file checksums. | Two builds in the same minute had the same version, so the app ignored the second. Found while running the demo loop. |
| 2026-09-18 | Accepted | The GitHub repo is public (Kyle). Swept first: no staff names, keys, tokens, or email addresses anywhere in history. | — |
| 2026-09-18 | Accepted | New categories `rec.center` and `rec.library`. They appear under Recreation and answer "It's too hot or too cold." Rec center listings say a membership is needed for programs and that anyone may come in to cool off, which is what the City's pages say; they are not called "free." | The City requires memberships for programs; its heat-relief page opens all rec centers during normal hours. |
| 2026-09-18 | Superseded 2026-09-19 | A church that hosts a food-bank distribution may be listed when the **church's own site** publishes it. We still never copy Forgotten Harvest or Gleaners partner lists. | The host is the owner of that fact; this is what closes the west-side gaps. |
| 2026-09-18 | Accepted | The app checks for a newer list at start, when it comes back into view (at most every 15 minutes), and when the phone comes back online; it also follows links opened while it is already running. | An installed app left open would otherwise never update. Found while verifying new listings. |
| 2026-09-18 | Accepted | **Search** is one shared rule in `packages/query` (spec: "Search" in `schema/query-spec.md`, fixtures `10-search.json`): every typed word must start a word in the listing; name matches first, then organization, then what / who / street / ZIP; then the one ranking rule. Places closed in the last 90 days are matched by name and shown apart under "Closed places." The typed text lives in one variable: never stored, sent, or put in the URL, and cleared when a tab is tapped. | docs/05 "Search"; with 120+ listings a person who knows a name needs a faster path than categories. iOS re-implements against the same fixtures. |
| 2026-09-18 | Accepted | **"Type a ZIP"** (docs/05): the bundle carries one center point per ZIP (`places/zips.json`, 34 ZIPs, about 1 KB, rounded to about 100 m) from the City's open-data ZIP areas layer (`pnpm ingest:opendata`, committed as `data/ingested/city_zips.json`). The typed ZIP is looked up on the phone and held in memory only. A ZIP never draws the "you are here" dot on the greenway map. Border ZIPs whose center sits just outside the city bbox are kept (0.05° slack). | A person who won't share a location, or whose phone can't get one, can still sort by distance. No request carries the ZIP. |
| 2026-09-18 | Superseded (all four built: see the Saved, Add a place, Help paying for food and Photos rows) | Still not built in the PWA: Saved (sensitive categories need the 10-A8 rule first), "help paying for food" link-outs, add-a-place form, condition-report photos. | Supersedes the search and ZIP items in the earlier "Not built yet" row. |
| 2026-09-18 | Accepted (replaces the "Proposed" PMTiles + MapLibre row, and the inline-SVG greenway map) | **The app draws its own street map from City of Detroit open data**: roads with names, park outlines, city boundary, prepared by `pnpm ingest:basemap`, shipped in the signed bundle, drawn on one canvas with drag, pinch, +/- buttons, keyboard, a full-screen mode, street and park names, and tap-to-select for the greenway, listing dots and parks. Used on Recreation, the greenway, each segment, the parks list, and each listing with coordinates (never a sensitive listing). The map data is about 140 KB gzipped and loads only when a map first opens. | Kyle, 2026-09-18: people must see cross streets and parks. Same privacy result as B8 (no third party ever sees where a person looks, works offline) without a 200 KB+ WebGL library, a 50 MB tile file to host, or an ODbL attribution chain; a cheap phone draws it fast. PMTiles stays possible later if we ever need buildings or water. |
| 2026-09-18 | Accepted | Greenway segments carry `cross_streets`, worked out by the pipeline (a street that meets the path at more than 30 degrees; freeways and tunnels excluded), and the segment screen lists them as "Streets along the way." | The map is never the only way to get a fact (accessibility, and "must work with the map failing"). |
| 2026-09-18 | Accepted | The map files are fetched whole, not tile by tile, and the map never moves to the person's location by itself. A typed ZIP is never drawn as "you are here." The full-screen map sits under the top bar so Urgent help and quick exit stay one tap away. | Tile-by-tile loading would tell our own host roughly where a person is looking; two files tell it nothing. |
| 2026-09-18 | Accepted (known limits) | Map limits: no water or buildings (the river shows as the area outside the city boundary); Hamtramck and Highland Park show main roads only; the roads layer was last edited 2025-09-30. The nightly publish runs `ingest:basemap` on the 1st of the month. | The City's layers cover the city only. |
| 2026-09-18 | Accepted | Help lists (categories and needs) offer "Show these on a map": closed until asked for, so the first Call button stays near the top; every listed place with coordinates is a dot that opens its details. Never offered on the "not safe at home" screen, and sensitive listings never get a dot. Maps about help hide the small-park dots so the listing dots stand out. | "All maps in the app" (Kyle, 2026-09-18). The three-taps-to-a-phone-number rule still holds. |
| 2026-09-18 | Accepted | **Saved places**: listing ids in on-device storage only, never sent; "Remove all" button; the screen and its link are named plainly and leave no URL. Domestic-violence and mental-health listings cannot be saved at all; every save shows "Anyone who uses this phone can see it." | docs/05 "Saved" plus audit A8: a phone may be looked through by someone else. |
| 2026-09-18 | Accepted | **Add a place** is one screen, not two, with hours as plain text ("every Tuesday 10 to 12") instead of a schedule picker. No choice exists for a domestic-violence shelter. Sends exactly the API's closed schema; queues on the phone when offline; the thank-you says a person will check it and that it can take days. | The steward checks and enters the schedule anyway (docs/04), so a picker would only add ways to get it wrong. Differs from docs/05's "two-screen form; schedule picker." |
| 2026-09-18 | Accepted | **Help paying for food** is a third choice under "I need food": link-outs to MI Bridges (SNAP), Michigan WIC, and Double Up Food Bucks, each opened on 2026-09-18. No phone numbers, no eligibility rules copied. | docs/02: benefits are "link-outs, not locations." |
| 2026-09-18 | Accepted | **Neighborhood pages live inside the app** (`#/n`, `#/n/nbh_…`), not as a separate site: one signed file, `indicators/neighborhoods.json` (about 55 KB gzipped), downloaded only when a neighborhood screen opens. Reached from About and from a greenway segment; never from the Help tab. New id prefix `nbh_`. A copy without outlines is committed to `data/indicators/neighborhoods.json` on each build so every number's history is in git. | docs/13 allowed "static HTML/JSON on the same host"; one file keeps the same trust chain as everything else and tells our host nothing about which neighborhood a person reads. |
| 2026-09-18 | Accepted | Sales and permit numbers come from **server-side statistics queries** grouped by the City's own `neighborhood` field (no point-in-polygon, no record downloads). Suppression happens at ingest: a count under 5 is stored as `lt5`; a median needs 10 sales. Every neighborhood is marked "coverage not checked" until a steward does a coverage pass, so every help panel leads with "we haven't listed much here yet" and the add-a-place button. | Honesty rules 2 and 6; and the Assessor's layer carries buyer and seller names, which must never enter the repo. |
| 2026-09-18 | Open | Indicators not built: ~~crashes~~ (built 2026-09-20, see the "Safe streets" row), CDC PLACES tract map, foreclosures and evictions (source not found), council-district lens pages, a record of which neighborhoods have had a coverage pass. | docs/13 steps 4 to 6. |
| 2026-09-18 | Accepted | Alerts are written with `pnpm alert:new`, which enforces: an end time (7 days at most), an https source link, a valid phone number. A **demo** alert is labeled "Demo" in its title and body, lasts at most 3 hours, and cannot carry a phone number. The alert card shows when it ends and links to where it was announced. | A made-up emergency in a public bundle would be a false statement; the demo script (docs/09) still gets its live auto-expiry. |
| 2026-09-18 | Accepted | Neighborhood "Conditions" panel: blight tickets per 1,000 parcels (parcel counts from the Assessor's layer), demolitions, and Improve Detroit issues with median days to close. Issue types are an allowlist of four things-not-people types; "Squatters Issue" and abandoned-vehicle types are excluded. Same server-side counting: the blight layer's owner and inspector names are never requested. | docs/13 step 4, honesty rules 3 and 5, and docs/11 "things, never people." |
| 2026-09-18 | Accepted (hackathon slice of docs/11, demo-only until the legal advice in docs/11 is in hand) | **Photos on condition reports.** Phone: camera capture, re-drawn on a canvas at 1280 px at most, then every block that is not picture data is cut out (Chrome adds a color profile to canvas JPEGs). Server: `POST /v1/photos` accepts only a JPEG made of picture segments (any Exif, XMP, ICC, IPTC or comment block is a 422), drops bytes after the end of the image, stores it in a private R2 bucket under a random key, and records only the key and the minute. The report then names the key; photos are accepted on place reports only. Stewards alone can see a photo, and can delete it at once; the daily job deletes it 30 days after its report closes, or after one day if no report claimed it. No public route serves a photo. The server does not re-encode (a Worker cannot without a codec); the strict structure check is what stands in for it. No face detection yet: capture copy plus steward review. | docs/11 "Hackathon slice" item 4. Found by testing against the real API, not the unit tests: the browser's color-profile block. |
| 2026-09-18 | Open | Before photos are on for the public: legal advice (docs/11), the R2 bucket (needs Kyle), Turnstile or app attestation, on-device face block-out. Until the bucket exists the Worker answers 503 and the app sends the report without the photo and says so. | — |
| 2026-09-18 | Superseded by the 2026-09-20 row "Safe streets is built from SEMCOG's crash layer" | docs/13 "Safe streets" (crashes involving people walking or biking) is not built: the City's `Traffic_Crashes` layer holds 2011 only (19,584 rows, checked 2026-09-18), and current data lives in the State's Michigan Traffic Crash Facts query tool, which has no open API. Showing 2011 numbers as if current would break honesty rule 7. Next step: ask MSP/UMTRI for a yearly extract, or a person downloads it once a year. | — |
| 2026-09-18 | Accepted | **Spanish UI** (docs/09 v1). `strings/es.json` has every key of `en.json` (tests check keys, placeholders, and that 911/988/211 survive). The app picks Spanish when the phone's language is Spanish, and a "Español / English" switch on Home and About changes it; the choice is kept on the phone only. What a place wrote about itself stays in English, marked `lang="en"`, with one line saying so: safety facts are never machine-translated (docs/05). Needs a native speaker's review before launch; the translator's 15 least-sure strings are in the session notes, starting with the overdose steps. | docs/05 "Language". |
| 2026-09-18 | Accepted | Every calendar date the pipeline writes (checked on, fetched on, last edited, today for validation and for dropping past events) is the **Detroit** date (`util.today`). Found when an evening run stamped listings with tomorrow's UTC date and would have dropped the day's events after 8pm. | CLAUDE.md: America/Detroit everywhere. |
| 2026-09-18 | Superseded (by "The City had the six overdose steps approved", 2026-09-20) | Overdose steps follow national guidance and still need review by DHD, MDHHS or counsel before a public release; the copy must never promise legal protection (docs/08, 10-A7). | Moved out of the superseded "Not built yet in the PWA" row. |
| 2026-09-19 | Superseded (by "Stale rows corrected by the audit", 2026-09-20) | **Not built:** page watchers, the nightly `check:sources --recheck`, and machine-raised steward tasks. `publish.yml` does not run `check:sources`; open-data changes arrive as a pull request instead. Many docs still describe watchers as the mechanism; they stay in the design as planned. | Found by the docs audit, 2026-09-19. |
| 2026-09-19 | Accepted | **Dependencies and CI brought up to date:** Vite 8, Vitest 5, TypeScript 7, Wrangler 4, `@noble/ed25519` 3 (bundle signatures now checked in strict RFC 8032 mode), `csv-parse` 7 (all seed rows parse byte-identically). `@types/node` stays on the 22 line to match the Node we run. GitHub Actions moved off the retired Node 20 runtime; runners pinned to `ubuntu-24.04`; the Swift tests run in the official `swift:6.3-noble` image. | Nothing may silently change behavior: each upgrade was checked (ES2020 output parse, planted type error, identical CSV parse, dry-run deploy, live report). |
| 2026-09-19 | Accepted | **pnpm 12.** Dependencies may run install scripts only when named in `pnpm-workspace.yaml` (`esbuild`, `workerd`). A package version must be a day old before it installs; the newest Wrangler and `@types/node` were hours old, so they are held one release back. The publish job runs without a dependency cache, because it holds the signing key. | Supply-chain safety for a project that signs what phones trust. |
| 2026-09-19 | Open | **detroitmi.gov now answers 403 to scripted requests**, including our honestly labeled pipeline. Effects: City events keep the last good file; the shelter line's source page can't be machine-checked, so after 2026-10-18 a release build needs a person's check (a logged call, or a read in a browser recorded in `emergency.csv`); 33 listings whose page is on a blocking site carry a "check by eye" note. We do not disguise our requests as a browser. Options for Kyle: ask the City to allow the pipeline, or check those pages by hand on a schedule. | Found in the 2026-09-19 data refresh. |
| 2026-09-19 | Accepted | **No artificial timers (Kyle).** A listing's wording and place in a list change only when people report something, never because time passed. Badges keep stating dated facts ("Matched their website when added, Sep 18, 2026"), so a reader can judge; the check-by cadences (45/60/180 days) and the 90-day "source list went quiet" switch are removed from the badge and from ranking. Nothing ever disappears except through a person's report and a steward's archive. Supersedes the cadence rows (harm-reduction 30 days, "No scheduled verification" aging) and query-spec's freshness tiers as a ranking key. | Kyle, 2026-09-19: "I don't want all the resources to disappear unless there is good reason, i.e. a person telling the app that the resource no longer exists." |
| 2026-09-19 | Accepted | **The phone's own copy:** the note "Your phone last got updates N days ago. Call before you go." shows on every list and listing, not only Home. It states a fact about the phone (it may be missing recent reports), not about a listing. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | **Shutdown only by a person.** The heartbeat and the automatic 120-day sunset are removed. A steward retires the directory on purpose by publishing a final list marked retired; the app then says "This list is no longer being updated. Call 211." and hides report buttons. Supersedes the dead-man-switch trigger in docs/12 and the heartbeat row. | Kyle, 2026-09-19: no artificial timers. |
| 2026-09-19 | Accepted | **Report counting:** one phone counts once per listing per day across all closure kinds ("Closed for good" and "Moved" together), so "2 people said this was closed" means two phones. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | **Nightly re-check** of every listing's own page, with the fixed matcher: a page that stops showing the listing's phone or street number puts the listing at the top of the steward queue ("their page no longer shows this"). The app does not change until a person decides. Replaces "page watchers" as the mechanism. | Kyle, 2026-09-19: reacts to a real change, not a timer. |
| 2026-09-19 | Accepted | **Emergency numbers:** a release build fails only when a number's own page is read and shows a different number (a mismatch). A page that can't be fetched (blocked, down) does not stop a release; it is logged for a person. 911 and 988 stay hardcoded. Supersedes the 30-day match rule in the "Use the currently published numbers" row. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | **DV shelters: never an address, distance, or map dot**, even when a shelter publishes its own address. docs/08's "unless listed publicly" exception is removed. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | **"A safe place to sleep → I'm under 25"** shows youth shelters first (Covenant House, Ruth Ellis Center, National Runaway Safeline), then regular shelters; no after-school programs on that screen. Covenant House is filed as a shelter with the youth flag. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | Greenway and park files keep downloading with the core list, so Recreation works offline even if never opened online. docs/11's "fetched after the help categories" is withdrawn. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | **Photos have a switch, off by default** (Worker variable). Until it is on, the photo field is hidden and `POST /v1/photos` answers 503; reports work without photos. Turn on only after the legal advice in docs/11. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | **Add the Detroit Health Department's programs** (WIC, vaccinations, lead, SisterFriends, HIV/STI testing, vital records and the rest listed in docs/02), each researched on DHD's own pages and checked by a person in a browser. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | **Android is the web app** (installable from a link; no store app for now). | Kyle, 2026-09-19. Analytics stays an open question for later; today none are collected. |
| 2026-09-19 | Accepted | **City events dropped until there is a real feed.** detroitmi.gov now runs Cloudflare bot protection: people's browsers pass, programs don't, and we don't defeat it. The pipeline stops reading the City calendar, the bundle carries no events, and the Events tab and Home's "Coming up" hide themselves when there are none. Listings whose source page is on detroitmi.gov (25, plus the shelter hotline page) are checked by a person in a normal browser. | Kyle, 2026-09-19. The site loads for people and blocks our pipeline since 2026-09-19. |
| 2026-09-19 | Accepted | **"I'm under 25" lists emergency shelters, youth shelters first.** The shared ranking gained one optional key, `prefer` (rows with the preferred flags first; nothing left out), in the spec, TypeScript, Swift and fixtures. Covenant House moved from `youth` to `shelter.emergency` (it keeps the `youth` flag). The route used to list after-school programs. | Implements Kyle's decision above; `prefer` rather than `flags` so an 18-to-24-year-old still sees every shelter. |
| 2026-09-19 | Accepted | **Schedules are validated strictly and never guessed at.** Exact `HH:MM` (24:00 max) and real dates; `interval` ≥ 1; BYDAY only with WEEKLY/MONTHLY, numbered only MONTHLY; BYMONTHDAY only MONTHLY. The pipeline refuses the rest; clients skip a bad schedule and show "unknown" if none are left. | Keeps the web (rrule) and iPhone (own evaluator) from disagreeing on shapes only one of them understands. All current data is WEEKLY. |
| 2026-09-19 | Accepted | **Proposals are deleted 180 days after a steward settles them** (accepted, rejected or duplicate), in the same nightly pass as reports. An open proposal waits for a steward. The steward's logged action stays. Replaces docs/08's "no retention limit yet." | Review weakness 14: proposals were kept forever. An accepted place lives on in `data/seed/`, so the proposal row adds nothing after that; 180 days matches reports. |
| 2026-09-19 | Accepted | **Masking covers every free-text field:** a proposal's "what" and "days and times," and a report's suggested hours and address, as well as the notes. The place's own public phone fields (a proposal's phone, a suggested phone), its name and its address are kept as typed. Changes docs/08, which said a proposal's other fields were not masked. | Review weakness 14. A number the place publishes still reaches the steward through the phone field. |
| 2026-09-19 | Accepted | **Condition reports keep only the hour** they were received as well as observed, and so does a photo's upload time. Listing reports keep the minute. | Review weakness 13 and docs/11: the minute a place report arrives is the minute the person stood there. |
| 2026-09-19 | Accepted | **The nightly retention pass takes a report's photo with it:** the picture is deleted from the bucket first, then counting, removing the photo rows and removing the reports run as one D1 batch. Without the bucket binding, a report with a photo waits. A photo is claimed by a report in the same batch that stores the report, so two reports can't share one. | Review weakness 12: one photo row blocked the delete (foreign key) after the counts were already added, so the counts doubled every night. |
| 2026-09-19 | Accepted | **Restoring a listing records `restored`, never `confirmed_by_phone`,** in the override, the steward log and the reports it settles; the restore call refuses any other reason. `pipeline/src/reports-sync.ts` no longer turns an `active` override (restored, or an older confirmed_by_phone default) into a phone check; it only clears the rejected closure reports. | Review weakness 10b; CLAUDE.md: never say verified for something no person checked. |
| 2026-09-19 | Accepted | **Steward writes refuse cross-site requests:** every non-GET under `/v1/steward/` needs `content-type: application/json` (else 415) and an `Origin` equal to the Worker's own origin (else 403). The pipeline's Access service token is exempt from the Origin check (it rides in headers no other site can set, and a script sends no Origin); the local `DEV_STEWARD` login accepts any `http://localhost` or `127.0.0.1` origin, because Vite serves the page on another port. `apps/web/public/_headers` sends `frame-ancestors 'none'` and `X-Frame-Options: DENY` for `/admin`. | Review weakness 15. Whether the Access cookie is SameSite is not ours to control, so the check lives in code. |
| 2026-09-19 | Accepted | **A steward settles only the reports the page showed.** The page sends their ids: `POST /v1/steward/reports/settle` (one target; only those ids, only while open) and `report_ids` on the archive/restore call (only closure reports among them). A report that came in after the page loaded stays open. Without `report_ids` the status call settles none. | A button used to re-read the queue and settle whatever was open, including a "closed" report sent a second earlier, which a restore then rejected unseen. |
| 2026-09-19 | Accepted | **Confirmations ("Still open", "Looks good") are never in the steward queue and are never settled** by any steward call; they stay open for the badge until the 180-day pass. The "2+ said closed" highlight uses the Worker's count of different phones (`closed_phones`), and `wrong_open` in the aggregates counts different phones too. The hashes never leave D1. | One phone = one report (above). Settling a listing used to settle its confirmations, which wiped `last_confirmed_at`. |
| 2026-09-19 | Accepted | **Access keys: fetch again when a token names an unknown key, at most once per 5 minutes.** Keys are kept for an hour; a failed fetch keeps the old keys and waits the same 5 minutes. | Access rotates its signing keys; before this, stewards were locked out for up to an hour after a rotation. The limit stops a flood of made-up key ids from hammering the certs endpoint. |
| 2026-09-19 | Accepted | **Re-check tasks** (implements "Nightly re-check … produces a steward task only"): new D1 table `steward_tasks`, ids `task_` + 16 random hex. Only listings `active` before the check get a task (result `missing` or `unreadable`; detail is the matcher's own words, 300 characters at most). Each night's list replaces the open set; a listing not reported closes as `resolved_by_check`. A steward's "Checked: it's fine" / "I'll fix it" holds while the check says the same thing: same result, and for `missing` the same detail (an unreadable page's reason varies night to night, so only the result counts). After the page has matched once, a later miss is a new task. `checked_on` is the day the current result was first seen. | Keeps a steward from dismissing the same miss every night, without hiding a new one. |
| 2026-09-19 | Accepted | **The re-check list lives at `data/staging/recheck.json`, git-ignored,** and the two re-check steps run last in `publish.yml`, even after an earlier step failed, and can't fail the job. `tasks:sync` sends nothing if the list is missing (an empty list would close every task). The notes `check:sources` writes into `resources.csv` on the runner are not committed. | `data/staging/` is committed and swept into the nightly open-data pull request; a machine report doesn't belong in that diff. |
| 2026-09-19 | Accepted | **Service area: Detroit, Hamtramck, Highland Park and Dearborn.** Places in these four cities are listed; other suburbs stay out. The bbox widens west to −83.33 for Dearborn. "Type a ZIP" adds 48120, 48124 and 48128 from the Census Bureau's ZIP Code Tabulation Areas (public domain), since the City's ZIP layer covers Detroit only. Known limits: the street map, parks and neighborhood pages are Detroit's own City data, so Dearborn shows no streets or parks yet, and Hamtramck and Highland Park show main roads only; the shelter line and the other urgent numbers are Detroit's (see the open question on urgent numbers). | Kyle, 2026-09-19: "I want to include Hamtramck, highland park and Dearborn." |
| 2026-09-19 | Accepted | **Two shelter lines, labeled by city.** CAM, the Detroit Continuum of Care's intake (the City's 866-313-2520), serves Detroit, Hamtramck and Highland Park (camdetroit.org says so); Dearborn is in the Out-Wayne County Continuum of Care, whose housing crisis line is 734-284-6999 (outwaynehomeless.org/get-help, read in a browser 2026-09-19: the site answers scripts with 403). Both show on the urgent sheet and the shelter screen, each labeled with the cities it serves. The crisis line (DWIHN, all of Wayne County), the domestic violence hotline, 211 and 988 already cover all four cities. | Follows the service-area decision above. |
| 2026-09-19 | Accepted | **The map covers all four cities.** Streets in Hamtramck, Highland Park and Dearborn, and the outlines of all four cities, come from the US Census Bureau's TIGER data (public domain; no share-alike terms, unlike OpenStreetMap). Detroit's streets and parks stay the City's own. The four outlines come from one source so their borders line up; Hamtramck and Highland Park no longer read as water. The map grid is pinned to its original origin so it doesn't move with the service area. Parks outside Detroit are not drawn yet (no public-domain parks layer found). This supersedes the service-area row's map limit. | Follows Kyle's service-area decision; no new license. |
| 2026-09-19 | Accepted | **Monthly days may be entered as MONTHLY schedules by hand** after `import:lines` ("3rd Saturday" → `3SA`, "1st and 3rd Wednesday" → `1WE,3WE`), and a seasonal closing as `valid_from`/`valid_to` (Dearborn's Centennial Library Sundays end 2027-05-24 unless renewed). Only when the page states the day and time exactly; the file's header comment says which rows. | The line parser reads weekly hours only; the query spec already supports these fields, and "call first" for a stated monthly time hides a fact. |
| 2026-09-19 | Accepted | **Narcan boxes from a seed list are `unknown` when their page gives no hours**, like DHD's stations, not "call first": you can't call a box. They keep their own entry-check badge; the station-badge test now picks DHD's open-data rows by source type. | Dearborn Public Health's boxes are the first non-DHD Narcan rows. |
| 2026-09-19 | Accepted | **A "still open" tap clears a closed label only by matching the count:** the label stays until as many different phones say "still open" after the latest closed report as said closed (`open_after_closed`, counted in the Worker), or until a person's phone check on a later day. A newer confirm date alone no longer clears it. | Kyle, 2026-09-19 (review item 18): one fake tap must not undo real reports. |
| 2026-09-19 | Accepted | **While the circuit breaker is tripped, labels freeze as they were:** the Worker counts only reports made before the 24-hour burst window, so a flood of closed reports neither adds labels nor hides real ones, and a flood of "still open" taps waits too. The pipeline applies the counts as sent. Steward decisions still apply. | Kyle, 2026-09-19. Before this, a tripped breaker hid every closed label, so a flood could hide real closures. |
| 2026-09-19 | Withdrawn same day | **Forgotten Harvest mobile pantries are listed as the food bank's own stops, call-first ("some Fridays 9am-noon")**, because its find-food page lists dated stops two weeks at a time without saying how often each repeats. A MONTHLY schedule is added only when the host's own page states the pattern (Historic Little Rock: "2nd & 4th Fridays"), with the time from Forgotten Harvest's page. | Districts 5 and 6 batch: mobile stops are the only food listed in several gaps, and a dated stop copied as weekly would be a guess. | **Withdrawn:** it conflicts with the 2026-09-18 row (never copy Forgotten Harvest or Gleaners lists); the six rows went back to proposed until each host's own site announces the distribution.
| 2026-09-19 | Accepted | **Forgotten Harvest and Gleaners own the facts about their own distributions** (their mobile pantry stops and their partner-pantry lists). Their own pages count as a listing's source, like any owner's page: the strict matcher must still find the phone and street address there, and a page only a browser can draw (pantrynet.org) is checked by a person. A host's own site is still fine, and preferred when it states the days more fully. Supersedes the 2026-09-18 row; the withdrawn Forgotten Harvest row's six listings may be sourced from forgottenharvest.org. | Kyle, 2026-09-19: "Forgotten Harvest and Gleaners should be the owners." |
| 2026-09-19 | Accepted | **Six more neighborhood measures** (Kyle approved the list): stores that take a Bridge card and bus stops (distance and count within half a mile; points committed as coordinates only, no names; the 28 Restaurant Meals Program places left out, and the nearest grocery store shown next to the nearest store of any kind); active rental certificates of compliance and vacant-building registrations (today's counts per 1,000 lots, not per year); fires per year per 1,000 lots, **building fires only** (17 Fire Department call types, one count per fire via `exposure_number = 0`; car fires out because before 2025 "Automobile" can't be told from a crash call; outdoor fires, medical calls and false alarms out); and the share of rated main-street length in poor condition (1 to 4 of 10; each piece's real rating year, freeways out, a piece goes to the neighborhood around its halfway point). Each shows its caveat next to the number. | docs/13 honesty rules; sources on data.detroitmi.gov, checked 2026-09-19. |
| 2026-09-19 | Accepted | **Every relevant phone number, each labeled with what it's for.** A listing carries up to two numbers with plain labels ("Pantry appointments", "Forgotten Harvest main line"); a number that belongs to someone other than the listing says whose it is. A second number published on another owner's page carries `phone2_source_url` and is checked on that page. Forgotten Harvest stops: the host's number where the host publishes the distribution, plus Forgotten Harvest's main line (248-967-1500); its Community Choice Market appointment line (248-268-7756) is not listed on truck stops. | Kyle, 2026-09-19: "if there are different phone numbers, accurately list and describe all relevant ones." |
| 2026-09-19 | Accepted | **Which numbers count as relevant** (from the sweep of every listing): intake, appointment, 24-hour, text/TTY, per-site and service-specific lines on the listing's own page. Left out: fax, other programs of the same organization, donation/volunteer/admin lines, numbers already on the urgent sheet (`emergency.csv`), and any line in a named staff member's name (personal; e.g. LAHC's pantry scheduling line, which Kyle may overrule). A number that belongs to someone other than the listing says whose it is ("City of Dearborn main line (311)", "Wayne Metro water help (WRAP)", "CAM intake line"). Listings with a third relevant number keep the two most useful. | Kyle, 2026-09-19: "accurately list and describe all relevant ones." |
| 2026-09-19 | Accepted | **The app is named "313 Help"**, everywhere: the strings (both languages), the web manifest, the page title, the steward page, NOTICE, docs, and the internal names (`@313help/*` packages, the `313-help` repo, Worker and D1 database, the phone's local storage, the `HelpApp` iPhone folder and `Help313App` type). Web and search checks 2026-09-19 found no "313 Help" organization or app; 313help.com/.org/.app had no DNS records. Nearest in the same space: 313SafeBeds (shelter-bed availability); Kyle knows its founder and will coordinate (and ask about sharing bed availability). Not yet checked: Michigan's business registry and USPTO. | Kyle, 2026-09-19: the earlier name was too close to a local organization's; he chose "313 Help" and asked for no references to the old name. |
| 2026-09-19 | Accepted | **Credit everyone.** NOTICE names every data provider, all 92 organizations whose own websites supplied listings, the urgent-number owners, the standards and the open-source software with their licenses; the app's About screen has a short "Where our information comes from" section with the count of sources taken from the list itself. | Kyle, 2026-09-19: "credit everyone". |
| 2026-09-19 | Superseded by the 2026-09-20 row "Safe streets is built from SEMCOG's crash layer" | **SEMCOG crash data waits until after the hackathon.** The email about its terms (attribution wording, indemnification) is drafted; crashes stay unbuilt until then. | Kyle, 2026-09-19. |
| 2026-09-19 | Accepted | **Linwood Technologies LLC is named as the maker**, with kyle@linwoodtechnologies.com for questions and corrections, on the About and privacy screens and in NOTICE. | Kyle, 2026-09-19: "put in Linwood Technologies". An organization's contact, not a resident's; the zero-PII rules are about residents. |
| 2026-09-19 | Accepted | **GitHub repo renamed to `313-help`** (github.com/kp2485/313-help; GitHub redirects the old address). The not-yet-created photo bucket is now `313-help-photos` and the publish job's git name `313-help-publish`. Local folder names and internal package names are unchanged. | Kyle, 2026-09-19, with the app's new name. Supersedes the "internal names stay" note for the repo only. |
| 2026-09-19 | Accepted | **New kinds of help to research and add:** jobs and job training (Detroit at Work, SER and the rest), drug and alcohol treatment people can check into voluntarily, rent/eviction/homeowner help, free legal help, ID documents, help after sexual assault, free tax preparation, help signing up for benefits, free clothing and baby needs, daytime drop-in centers, free phones and internet, dentist and glasses, help paying for child care, rides to appointments, low-cost vet care. Research notes go in `docs/research/`; listings enter the usual way (own page, `check:sources`). | Kyle, 2026-09-19: "I want to make sure we include job training and finding resources", "look up drug rehab type centers that people can voluntarily check into", then "I want to do all of these" for the rest of the list. |
| 2026-09-19 | Accepted | **Treatment listings are private but findable:** they can't be saved and never go into the browser history (like crisis listings), and they keep their address, distance, map dot and directions, because people have to get there and a treatment center's address doesn't endanger anyone the way a DV shelter's does. The detail screen has the quick-exit button. | Kyle, 2026-09-19, choosing between this, the crisis-listing rules, and ordinary clinic rules. |
| 2026-09-19 | Accepted | **A shelter's address is published only if the shelter publishes it itself** on its own site. An address found anywhere else (a federal roster, a directory, a news story, a partner's page) is never used; the listing gives the intake phone instead. Applies to every shelter, youth and recovery residences included; DV shelters keep their stricter rule (never an address). The eight shelter rows in the seed were checked the same day: every address came from the shelter's own page (drmm.org, covenanthousemi.org). docs/08 updated, and its leftover "unless listed publicly" DV exception removed. | Kyle, 2026-09-19: "I definitely do not want to publish a shelter's address unless the shelter publishes it itself." Raised by Freedom House, whose address is on the DOJ roster while it publishes only a PO box. |
| 2026-09-19 | Accepted | **New categories** (docs/03): `jobs.find`, `jobs.training`, `learn.school`, `learn.english`, `treatment.crisis/.detox/.residential/.outpatient/.meds/.recovery`, `assault`, `health.dental`, `health.vision`, `housing.owner`, `shelter.day`, `goods.clothes`, `goods.baby`, `legal`, `ids`, `money.tax`, `money.benefits`, `kids.care`, `connect`, `pets` (plus the existing empty `housing.rent`, `harm.supplies` and `transport`). Sub-groups that don't change where you go are flags, not categories (`reentry`, `immigrants`, `paid_training`, `referral_only`, …). The Help tab gets a third group, **"Work, school, and paperwork"**; "I want help with drugs or alcohol" and "Help after sexual assault" go under **Right now**. Neighborhood pages count the new top-level categories too. | Implements Kyle's 2026-09-19 scope row above. One screen per question a resident would ask; a refinement only where it changes the list. |
| 2026-09-19 | Accepted | **Link-outs for every need, not just food.** Programs you apply for online (unemployment, Lifeline, the child-care scholarship, free PreK, Medicaid rides, reduced fares, IRS free filing, MARR's recovery homes, meeting finders…) are link-outs to the owner's own page, in `apps/web/src/links.ts`, shown after the list on that need's screen. Their words live in `strings/*.json` (`link.<set>.<id>.*`) so they are translated; the food links moved there from `benefits.ts`. A link with an application window carries `until` and hides itself after that Detroit day. Carrier pages count as the owner's page for the carrier's own low-income plan (AT&T Access); resellers never. | The old food list was English-only code. An expired "apply by" link is a small lie. |
| 2026-09-19 | Accepted | **Treatment and sexual-assault screens: numbers first.** Drugs or alcohol: DWIHN's ACCESS line (800-241-4949, now labeled "Mental health and addiction help, 24 hours"), the DWIHN Care Center walk-in (313-989-9444), then SAMHSA's National Helpline (800-662-4357). Sexual assault: Avalon Healing Center's 24-hour line (313-474-SAFE), VOICES4 (855-864-2374, call or text), then 911; Avalon is also on the urgent sheet. All four matched their owner's page (`check:emergency`, 2026-09-19). DWIHN numbers come only from DWIHN's own pages: three provider sites print wrong ones (800-421-4949, 800-231-1127, 800-841-4949), and a test keeps them out of `emergency.csv`. | docs/05 ordering rule (911/hotline first). Avalon asks survivors to call first and does not publish its exam sites. |
| 2026-09-19 | Accepted | **The app never says treatment is free for people without insurance** until DWIHN confirms it: DWIHN's pages say it serves uninsured residents, but its substance-use page reads as Medicaid-only. Only a provider's own "no one turned away for inability to pay" (Elmhurst) is repeated. | Never state what no page supports. One call to DWIHN settles it (Open, below). |
| 2026-09-19 | Accepted | **Recovery homes are a link to MARR's certified list, never listings.** Michigan does not license them, and each MARR entry names a staff person. | Patient brokering and uncertified "sober homes" are a known harm; MARR certification is the only public check. |
| 2026-09-19 | Accepted | **Import lines take an optional 13th field** of `key=value` pairs (`flags`, `phone_label`, `phone2`, `phone2_label`, `phone2_source_url`, `notice`), so a batch carries what a person used to type in afterwards. `phone2` needs a label. | 130 listings in one pass; hand edits after import were where facts slipped. |
| 2026-09-19 | Accepted | **For-profit schools are not listed** (e.g., Detroit Training Center, $3,000–6,000). Paid training reaches people through Detroit at Work's training link-out. Union apprenticeships are listed as link-outs while their application window is open (`until`). | "Free help" is the app's promise; an apprenticeship is a paid job but applying costs a fee and the windows are short. |
| 2026-09-19 | Accepted | **Birth certificates come from the Wayne County Clerk, not the Detroit Health Department**, which closed its vital-records office in December 2013. The "Add the Detroit Health Department's programs" row's vital records item points to the Clerk (held until a person reads the Clerk's own page; the county site blocks scripts). | Research, docs/research/2026-09-19-new-help/legal-and-id.md. |
| 2026-09-19 | Open | **Call DWIHN once** to ask whether it pays for drug and alcohol treatment for uninsured Wayne County residents, and whether 8726 Woodward (its home page) or 707 W. Milwaukee is the Care Center's address today. | Two facts the pages disagree on. |
| 2026-09-19 | Superseded (below) | **SAMHSA's treatment directory as a staged source.** The 2025 National Directory spreadsheet and the opioid treatment program list are public domain and need no registration (47 facilities in our four cities, no hours or walk-in field). They could feed `data/staging/` like the City layers, each row call-first until checked. FindTreatment.gov's API wants a named technical contact and fixed server addresses: not pursued. | Kyle to decide. |
| 2026-09-19 | Open | **Wayne County's naloxone and test-strip station map** (Dearborn 6, Hamtramck 5, Highland Park 2; updated 2026-09-14) would fill Narcan outside Detroit, but states no license. Ask the County about reuse, as with DHD. | The same "facts only, attributed" footing as DHD's layer, once asked. |
| 2026-09-19 | Accepted | **SAMHSA's lists feed treatment listings** (`pnpm ingest:treatment`, `pipeline/src/ingest-treatment.ts`): SAMHSA's National Directory spreadsheet (public domain) and live OTP list, matched against DWIHN's provider directory. Every program in the four cities is staged in `data/staging/samhsa_treatment.csv` with a decision and a reason. What a listing says (kind of care, medicines, Medicaid, sliding fee, Spanish, Arabic) comes from SAMHSA's codes; "women only" or "men only" only for live-in and detox programs; the survey's age answers and "special programs" codes are not treated as eligibility. Left out: sober homes (MARR link only), DUI-only programs, programs with no Medicaid, sliding fee, payment help or public funding. Import lines are written only when DWIHN names the program's own website, and `check:sources` publishes a row only if that site shows its phone and street number: the entry rule is unchanged. The run also warns about any treatment listing on none of the three lists (a possible closure). The spreadsheet is read with a small built-in zip reader (`pipeline/src/xlsx.ts`), not a new dependency. First run: 50 programs; 20 already listed; 9 imported, of which 5 matched their own site and are live and 4 wait for a person (their home page doesn't show the phone and street together); 20 more held with reasons in `to-verify.csv`; 1 left out. FindTreatment.gov's API is not used. | Kyle, 2026-09-19: "use samhsa's lists". |
| 2026-09-19 | Accepted | **Help tab layout:** "Right now" stays as full-sentence rows; "This week" and "Work, school, and paperwork" are two-column tiles with short labels (`tile.*`); "Browse every kind of help" is one row that opens to the category chips. Home's shortcuts add "Help with drugs or alcohol" and "A job or training" (six in all). A need whose list is empty but has links says "We don't list any places for this yet. These programs can help:" instead of sending people to 211 first. | With 23 needs the tab ran to five phone screens; now about three, with nothing urgent moved. |
| 2026-09-19 | Accepted | **Spanish loads only when chosen** (or when the phone's language is Spanish): a separate 17 KB file instead of part of every download. If it can't be fetched (no signal, never loaded), the app stays in English and the button does nothing. Main script 82 → 66 KB gzipped. | Cheap phones, bad signal; most readers never need both languages. |
| 2026-09-20 | Accepted | **211 is checked against its own page** like every other urgent number ("211" or "2-1-1" on mi211.org; matched 2026-09-20), and follows the same release rule: never checked is a warning, a mismatch stops a release build. Only 911 and 988, which are hardcoded, are never checked. | REVIEW weakness 30a: the gate skipped every three-digit number, so the one number a person is told to call when nothing else fits was never checked. |
| 2026-09-20 | Accepted | **The iPhone screens compile and run.** A hand-written Xcode project lives in `apps/ios/Xcode/` (git-ignored, recreatable from `apps/ios/README.md`): it references `HelpApp/*.swift` and both strings files in place, links the local `DetroitQuery` package, and copies `data/bundle/v1` in as the offline snapshot. One code fix was needed in two lines: `badge(row, now:)` bound to SwiftUI's own `View.badge` and is now `DetroitQuery.badge(...)`. `swift test` passes the 111 shared fixture cases before and after. Home, all three Help groups, refinements, results and the overdose screen render from the signed snapshot with no network. Known: Apple's location button draws its label clipped; network refresh, Spanish, calling, directions, events, greenway and search are untested; no icon, privacy manifest or signing yet. | REVIEW weakness 28 (the screens had never been compiled). This Mac has Xcode 27. |
| 2026-09-20 | Accepted | **`pnpm preflight` parses `emergency.csv` as real CSV.** It split lines on commas, so a label that contains one ("Sexual assault help, 24 hours (Avalon Healing Center)") shifted the columns and reported a mismatch that would have stopped a deploy for no reason. A test covers it. | Found by running preflight after the new urgent numbers were added. |
| 2026-09-20 | Accepted | **The "Not an official City of Detroit app" line is removed everywhere**: Home and About in both apps, both strings files, NOTICE, and the `pnpm preflight` check that required it. | Kyle, 2026-09-20, asked for the blurb to go. Noted so it isn't re-added by accident: docs/08 had it for App Store review and to keep the app apart from the City's own "Detroit Compassion App", so that distinction now rests on the store listing and on what we tell the City. |
| 2026-09-20 | Accepted | **The iPhone app follows the web design system** (`apps/ios/HelpApp/Palette.swift`): the same green palette in light and dark, cards on a tinted background, uppercase section headings, status pills whose colour follows the words, two-column tiles of one size for "This week" and "Work, school, and paperwork", a red 911 row, and call buttons that show the number. Apple's `LocationButton` is gone: it truncated its own label ("urrent Location"). In its place is an ordinary SwiftUI button with the standard CoreLocation flow, so the permission sheet still appears on the tap and never at launch, and a refusal says so in words. | Kyle, 2026-09-20: make it look more modern, native SwiftUI, uniform tiles. |
| 2026-09-20 | Accepted | **The greenway is drawn like a transit line** (`apps/web/src/map.ts`): one width the whole way with a casing underneath, so the route reads as a single line across phase changes; a colour and a dash for each phase (open solid green, being built orange dashes, funded blue dashes, planned grey dots, from `--gw-*` in the design system); station dots where stretches begin and end, once zoomed in past 14 m/pixel; and, on a segment's own screen, that stretch drawn bright with the rest of the route dimmed. A key under the map on the greenway screen names every phase in words, and tapping a stretch still says its phase, so colour never carries the meaning alone. | Kyle, 2026-09-20: "make the greenway look better on the map, maybe like a subway map". The old drawing was a thin green line with thin dashes that read as gaps rather than as one route. |
| 2026-09-20 | Accepted | **Stale rows corrected by the audit.** The 2026-09-19 row saying "Not built: page watchers, the nightly `check:sources --recheck`, and machine-raised steward tasks" is wrong for the last two: both exist (`pipeline/src/check-sources.ts --recheck`, `pipeline/src/tasks-sync.ts`, migration `0004_tasks.sql`, four Worker routes, all tested). What is true is that neither has ever **run**, because nothing is deployed and there is no workflow runner in this checkout. Page watchers remain not built and were replaced by the re-check (DECISIONS 2026-09-19). | [AUDIT-2026-09-20](AUDIT-2026-09-20.md), verified live against the local Worker. |
| 2026-09-20 | Resolved same day | **This working copy had lost its `.git` and `.github/`.** The publish workflow and CI described in README, docs/04, docs/06, OPERATIONS and CLAUDE.md are absent here, which is why one web test fails. The repository on GitHub had them all along. The history and the four missing files were restored into this copy on 2026-09-20, the suite went green (361 of 361), and the work was merged in pull request 1. | Found by the audit; Kyle confirmed the repo exists. |
| 2026-09-20 | Accepted | **The Worker deploys from Git with the root directory set to `api`; the site is published by `.github/workflows/publish.yml`, not by a Cloudflare Git build.** A Cloudflare build pointed at the repository root fails ("application detection logic has been run in the root of a workspace"): this is a pnpm workspace and the root has no Worker config. The R2 photo binding is commented out in `api/wrangler.toml` so a first Worker deploy succeeds; the code already treats the bucket as optional and answers 503 to photo uploads without it. | Kyle, 2026-09-20, after the first Cloudflare build failed. Deploying the site from Cloudflare would ship a dev-signed bundle and pin the dev key on every phone; the workflow signs a release bundle, pins the two real keys, and refuses to publish when an emergency number stops matching its page. |
| 2026-09-20 | Accepted | **Recreation and Transit become one Map tab.** Four tabs: Home · Help · Map · Events. The Map tab is the city map with a layer switcher (free help by group, parks, the Joe Louis Greenway, and every transport layer below), a text list of everything switched on, and then everything the two old tabs carried: the greenway, parks, recreation centres, trip planners, fares, free rides and transit phone numbers. Layers are real labelled checkboxes, keyboard-operable; the choice is remembered on the phone only (`apps/web/src/layers.ts`, IndexedDB) and never sent. Help layers are derived from the category taxonomy (`MAP_GROUPS`), and treatment, help after sexual assault, DV and mental-health-crisis listings are never drawn. Supersedes the five-tab row of 2026-09-18 and docs/05's tab layout. | Kyle, 2026-09-20: one map instead of two tabs. Two tabs each showed half the city; a rider looking for food wants the bus route on the same picture. |
| 2026-09-20 | Accepted | **New transport layers in the signed bundle, drawn on the device** (`pnpm ingest:transit`, `pipeline/src/ingest-transit.ts`): DDOT bus routes (87) and stops (5,098), SMART bus routes (86) and stops (4,412), QLINE stops (20), People Mover stations (13), MoGo stations (81), bike lanes (343), Amtrak stations (4), intercity bus stops (9) and park-and-ride lots (6). Each layer is its own file under `map/transit/` in the signed bundle, listed in `places/transit.json`, and downloaded only when a person switches that layer on. Gzipped: DDOT routes 10 KB, DDOT stops 54 KB, SMART routes 26 KB, SMART stops 51 KB, bike lanes 8 KB, everything else under 2 KB. No tile server, no runtime request to any of these owners, nothing sent about the rider. | Kyle, 2026-09-20: add transportation layers from public data. Geometry is simplified (7–9 m for bus routes) and clipped to the four cities plus about 7 miles, so one regional feed does not put three counties in the bundle. |
| 2026-09-20 | Open | **Licences to settle for the new transport layers.** Free and clear: the US DOT BTS Amtrak Stations layer ("a work of the United States government … available for unrestricted public use"). Flagged, and ingested meanwhile on Kyle's instruction to get the data in and review later: (a) every City of Detroit layer used here — DDOT Bus Routes, DDOT Bus Stops, QLine Stops, MoGo Stations, Bike Lanes — where the portal publishes a disclaimer and no licence grant, and the items leave `licenseInfo` empty; (b) SMART's GTFS feed, which publishes no terms at all; (c) the Detroit People Mover's GTFS feed, same; (d) MDOT Carpool Lots, whose terms are a disclaimer with no redistribution limit; (e) **US DOT BTS Intercity Bus Atlas Stops, which is CC BY-NC 4.0** — non-commercial and attribution-bound, which suits a free app but is a real condition Kyle should accept on purpose. Every layer names its owner and its licence text on the Map tab and in `data/ingested/transit/source.json`. | Kyle, 2026-09-20, for the hackathon. Each is a public agency's own open data, which is the bar CLAUDE.md sets; the gap is that most of them state no terms. |
| 2026-09-20 | Open | **DDOT's own GTFS feed is read by hand, not by us.** DDOT publishes `ddot_gtfs.zip` and its portal item marks it CC0 — the clearest licence of the lot — but `detroitmi.gov` answers scripts with 403 (the same bot protection that stopped City events and 33 listings, DECISIONS 2026-09-19). We do not disguise the request. Until a person downloads it or the City unblocks us, the bus layers come from the City's own DDOT Bus Routes and DDOT Bus Stops ArcGIS layers, which are not blocked. Their last edit was 2026-02-09, so a route changed since then is missing; the published GTFS was last valid to 2026-09-21. | CLAUDE.md: identify our requests honestly; if a site blocks them, check by hand rather than disguising the request. |
| 2026-09-20 | Superseded by the 2026-09-20 row "Safe streets is built from SEMCOG's crash layer" | **Crashes stay unbuilt.** SEMCOG's ArcGIS org does answer anonymously (`crash2024_10year`, 1,335,379 rows), but its portal publishes no terms for those layers and the source data is Michigan State Police CJIC, so it is not cleared; the row of 2026-09-19 deferring SEMCOG crash data still stands. The City's own crash layer holds 2011 only (2026-09-18). | Checked again 2026-09-20 while adding the transport layers. |
| 2026-09-20 | Open | **`pnpm ingest:transit` is not in the nightly publish workflow yet.** The basemap runs on the 1st of the month; the transport layers should run about as often (bike lanes were edited 2026-09-19, the DDOT layers 2026-02-09, SMART's feed 2026-08-26). Today they are refreshed by hand. | Noted so it is not forgotten; the workflow belongs to a different change. |
| 2026-09-20 | Accepted | **Two new kinds of help: `health.er` (Emergency room) and `health.urgent` (Urgent care).** They join the "I need a doctor, dentist, or eye care" screen, emergency room first, and the emergency-room list is led by **911** (a refinement can now carry its own emergency numbers: `refine[].first` in `apps/web/src/needs.ts`). Both are ordinary listings — an address, a distance and a map dot — and both sit in the health layer on the Map tab. An emergency room is shown as open all day and night only where its own page says so. Added to the taxonomy in docs/03, to the category check in `pipeline/src/validate.ts`, and to both strings files. | They cannot be `health.clinic`, which means **free or low-cost**: none of these places says it is, so filing a hospital emergency room or a for-profit urgent care under that label would make the app state something the owner's page never says. Research: `docs/research/2026-09-20/er-urgent-care.md`. |
| 2026-09-20 | Accepted | **The web app gets a laptop layout, and the phone layout is untouched.** Everything new sits inside `@media (min-width:64rem)` (and `hover:hover`/`print`), so a phone — and a laptop at 400% zoom, which reports a narrow viewport — renders exactly as before, pixel for pixel. On a wide screen the bottom tab bar becomes a rail down the side with **Urgent help first in it** (the top bar's copy is hidden, so the numbers are still one click from every screen); the rail is drawn *before* the page in the DOM at that width and after it on a phone, so what the keyboard walks is what the eye reads at either width, with a skip button ahead of both. The Map tab puts the map beside the layer switcher and the text list (`.maptop`/`.mapside`, `display:contents` on a phone so nothing about the one-column layout changes), the map staying put while the list scrolls. Cards, tiles and events take more columns; prose keeps one 42rem column. Hover states only where there is a real pointer; `:focus-visible` unchanged; Escape leaves the full-screen map; the canvas is drawn again when the pixel ratio or the window changes. A print stylesheet prints a listing as ink on white with each link's address spelled out. Everything added uses logical properties (`inset-inline`, `padding-inline`, `text-align:start`), for Arabic. Cost: +1.1 KB gzipped CSS, +0.4 KB gzipped JS. **Not done: list and detail side by side**, which would mean two screens in one history entry and a second way to open a listing; the router keeps one screen per entry and only a random key in history, and that rule is worth more than the column. | Kyle, 2026-09-20: make it work better on laptops without sacrificing the phone. |
| 2026-09-20 | Accepted | **A listing with coordinates but no street address still gets directions and a map.** Wayne County's naloxone and test-strip stations (`harm.supplies`) publish a point and no address; the detail screen used to dereference the address and would have thrown. Directions now live in `apps/web/src/directions.ts` and fall back to the coordinate (`maps.apple.com/?daddr=lat,lon`, `geo:lat,lon`, Google Maps, and the same for bus directions), and the "Where" block draws the map with a sentence naming the source in place of an address (`detail.where_no_address`, both languages). A coordinate is never printed as if it were an address. | Never turn a point into a street address the app cannot stand behind; and a station nobody can find is no use. |
| 2026-09-20 | Accepted | **A full WCAG 2.2 AA pass over the web app: 21 failures found and fixed, 4 left open with reasons** ([ACCESSIBILITY-AUDIT-2026-09-20](ACCESSIBILITY-AUDIT-2026-09-20.md)). The four that change how the app behaves: (a) **one live region, made once and kept outside `#app`** — every `role="status"` in the app was being rebuilt by `innerHTML` and therefore never announced, so nothing that happened without a screen change (report sent, report queued offline, place saved, key reset, ZIP not known, location refused, layer or language switched) was ever heard; (b) **the cursor goes back where it was** after any redraw that is not a new screen (`refocusSel`), instead of falling to `<body>` and restarting the keyboard at the top of the page; (c) **the full-screen map is a real overlay** — `role="dialog"`, everything else `inert`, Escape closes it and the cursor returns to the button that opened it; (d) **every screen has its own `<title>`**, except screens that must leave no trace, which keep the plain app name. Visual changes, all forced by contrast maths: a new `--edge` token for the outline of anything you press or type in (`--line` was 1.31:1, an empty text box had nothing else to show it was there), a deeper hero green (white on the old gradient was 3.39:1), darker main roads and freeways on the map, a stroked city boundary, and four pan arrows on every map. | Kyle, 2026-09-20: full accessibility audit, then fix what you find. Contrast is computed from the tokens in a test, in both themes, so it cannot quietly regress. |
| 2026-09-20 | Accepted | **The ZIP box says what it is for: `autocomplete="postal-code"`** (`apps/web/src/main.ts`, `locChip`). WCAG 1.3.5 asks that a field collecting something about the person name its purpose, and this is the only such field in the app. Nothing about the trade-off is hidden: the ZIP is still never stored by us and never sent, and the only new thing is that the browser's own autofill may offer a ZIP typed earlier on that device. | The alternative — `autocomplete="off"` — fails 1.3.5 and helps nobody. The residual risk is a shared phone (audit A8) and it lives in the browser's own UI, which the person controls; flagged for Kyle in case he wants it back off. |
| 2026-09-20 | Accepted | **Minor residential streets on the map stay below 3:1 (1.47:1 light, 1.44:1 dark).** Raising every street to 1.4.11's bar needs about `#8f9c94` on near-white, which turns the map into a grey slab and buries what actually carries meaning: the greenway, the layer lines, the listing dots and the street names (11.31:1). Main roads, freeways and the city edge were raised instead, and the edge is now a stroked line rather than a change of shade. The street mesh is context, not content: every map in the app is also a text list of the same places, and no fact is ever reachable only through the picture. | Noted openly rather than quietly, so Kyle can overrule it. Same reasoning covers `--map-out`. |
| 2026-09-20 | Accepted | **A fifth choice on "I need a doctor, dentist, or eye care": Health Department programs (`health.dhd`).** The category holds 24 live listings — Detroit Health Department clinics, shots, lead tests, WIC and wellness centres — and until now nothing on the Help tab queried it, so they could only be found by search or on the map. Strings in both languages (`refine.doctor.dhd`), and the refinement test enumerates it. | Coordinator, 2026-09-20. To be mirrored on iPhone and Android. |
| 2026-09-20 | Accepted | **Two edge protections are signed for by a person, and then proved from outside.** `api/edge-protections.md` holds one dated, named line for the Cloudflare Access application and one for the WAF rate-limiting rule. `pnpm preflight` reads them: until both are signed a deploy is blocked (`STOP`), and once a signature is 180 days old it asks for a fresh **look** — a warning, never a block, because a protection does not stop working on a birthday. `pnpm smoke -- https://<origin> --i-own-this-origin` then asks the live origin what preflight cannot see: that `/v1/steward/*` and `/admin/` refuse anyone not signed in, that an unknown field is a 400, that CORS names only our site, that photos are off, and that a burst of writes is rate-limited. It sends about thirty requests, every one of them a request the API must refuse, so it can never store anything, and it refuses to run without the origin flag. `api/test/scheduled.test.ts` holds the first cron run on a fresh database to changing nothing at all. Kyle signed both lines and committed the real D1 id on 2026-09-20 (a6e3520). | The two things the whole design rests on live in a dashboard no script can see (rate limiting is a WAF rule, 2026-09-18). Without this a checkout could pass every check and still deploy an unprotected API — the gap AUDIT-2026-09-20 names. |
| 2026-09-20 | Accepted | **Wayne County's Well Wayne Stations map is a publishable source: facts only, attributed.** 26 of its 97 stations are in the four cities (Detroit 13, Dearborn 6, Hamtramck 5, Highland Park 2); the nearest Health Department box to any of the 13 Detroit stations is 117 m away at a different site, so this is new coverage, not a second copy. It enters exactly as the DHD layer does — a tier A entry in `data/sources.yaml`, `mode: publish`, the `source_listed` badge, no per-station entry check — read by `pipeline/src/ingest-mymap.ts` (`pnpm ingest:mymap`) from the map's own KML, with the map's printed "Map updated" date as `source_last_edited` and the ingested CSV committed, so a changed point or name arrives as a git diff a steward merges. | Kyle, 2026-09-20: yes. Supersedes the 2026-09-19 Open row that asked whether to use it. |
| 2026-09-20 | Open | **Ask Wayne County about reuse of the station map.** Its page (endoverdosewayne.org) carries no terms of use and no licence statement, so `data/sources.yaml` records `license: unstated`, the same footing as the Health Department layer, and the rows publish as facts with the County named. Nobody has asked yet. **For Kyle.** | We publish another body's facts on the strength of its being that body's own open data; where no terms are stated, the honest move is to ask, not to assume. |
| 2026-09-20 | Accepted | **The Wayne County stations are `harm.supplies`, not `harm.narcan`.** Every station stocks naloxone (Narcan), fentanyl testing strips **and** xylazine testing strips, and the County's own legend is the only hours fact it gives (red = 24/7, purple = "not 24/7, usually Monday-Friday during normal business hours"). A station's `what` says what the County says, adds the printed box location, and repeats the page's own caveat that supplies depend on availability. | Test strips are precisely the difference between the two categories; filing them under "Free Narcan" would hide the thing somebody came for. |
| 2026-09-20 | Accepted | **An ingested layer may carry a city and no street address, and a coordinate is never reverse-geocoded.** `fromIngested` was written for the Detroit-only DHD boxes: it hardcoded `city: 'Detroit'` and required `address_1`. It now takes each row's own city and omits the address when the publisher gives none. A bundle row without an address is already legal (`fromSeed` omits it the same way) and the map dot is the publisher's own point. We never turn a coordinate into a street address and then print it as a fact. | The County publishes a site name, a city and a point for all 26 stations and no addresses at all. A station nobody can find is no use; an address we invented would be worse. |
| 2026-09-20 | Accepted | **A listing is findable by a phone, by a street address, or by its publisher's own coordinate — one of the three is enough.** The old rule (a phone or a house number) was written when every row came from a page with a door on it. | Otherwise a whole category of real help — a box on a wall, a rides line — is kept out by a rule about buildings. |
| 2026-09-20 | Accepted | **HSDS export: a location is `physical` when it has an address *or* a point, and `virtual` only when it has neither** (`pipeline/src/normalize.ts`). A Wayne County station is a real box you walk to; a hotline or a DV row with no address is virtual. | Exporting a station as virtual would tell every downstream consumer, 211 included, that there is nowhere to go. |
| 2026-09-20 | Accepted | **A ride program may publish with a phone and no street address.** The SMART Connector and the VA's Transportation Navigation Hub have no door to walk into, and their own pages print no street number. Anything with a door still needs its house number. | Requiring a house number would have kept real help out of a category that was empty. |
| 2026-09-20 | Accepted | **CHAG's Life Points is listed under `harm.supplies` on CHAG's own words and no more.** Its page names "Wound care training and supplies" and "Free NARCAN Kits & Training"; it does not name syringes or test strips, and the listing does not either. The 2026-09-19 hold on the six Detroit syringe service programs (CHAG, Detroit Recovery Project, ECHO, Safe Point/Team Wellness, CHASS, UNIFIED) was re-read on 2026-09-20 and stands: not one of the six names syringe services on its own page. | A state list saying a programme exists is not the programme saying so, and syringe services are exactly the fact a person would travel for. |
| 2026-09-20 | Accepted | **The iPhone app's origin and pinned keys are build settings, not code.** `DC_BUNDLE_BASE`, `DC_API_BASE`, `DC_PIN_ACTIVE` and `DC_PIN_SPARE` are set per configuration and read back through `HelpApp/Config.swift`. Debug points at `http://localhost:5173` and pins the dev key; Release ships placeholders, carries no `NSAllowsLocalNetworking`, and `Scripts/preflight.sh` fails the build until Kyle sets an https origin and two different, valid, non-dev SPKI Ed25519 public keys. The same rules exist twice on purpose — in shell for the build phase and in Swift (`releaseProblems`, `releaseKeyProblems`, a port of `apps/web/src/keys.ts`) for the app — and must be changed together. | Where a phone gets its list, and whose signature it trusts, should never be edited in a source file; and a release that would ship the dev key must fail loudly rather than install quietly. |
| 2026-09-20 | Accepted | **What the iPhone keeps lives in plain protected files, not the keychain and not `UserDefaults`.** The install key, saved places and the report outbox are files under `Library/Application Support/state/`. They are covered by the device's own file protection, they go when the app goes, and reading them needs no required-reason API — which is why `PrivacyInfo.xcprivacy` can truthfully declare none, no tracking and no collected data. | The keychain survives a delete-and-reinstall, which is the opposite of a random, resettable secret (docs/08). `UserDefaults` is a required-reason API and would have made the privacy manifest longer and less true. |
| 2026-09-20 | Accepted | **Three iPhone layout rules, from the Dynamic Type pass.** (a) A listing card's **Call button is drawn outside the card's link**, so tapping the card opens the listing and only the button dials. (b) A **phone number goes on a line of its own** once the text size reaches the accessibility sizes, and is never truncated or wrapped mid-number. (c) The **greenway keeps a tab of its own, titled `gw.title`**, because `tab.rec` was retired when the web app merged Recreation and Transit into the Map tab, and the iPhone has no map to merge it into. | CLAUDE.md: dynamic type must not truncate phone numbers — checked at `accessibility-extra-extra-extra-large` on Home, a results list, a listing, Saved places and Urgent help. A button nested inside a link is a coin toss about what a tap does, and the wrong side of that toss is an accidental call. |
| 2026-09-20 | Accepted (the "never compiled" half superseded the same day by the rows below: Kyle authorised the SDK licence, and `:app` then compiled, tested, assembled and ran on an API 35 emulator — never on a phone, and never below Android 15) | **An Android client exists in Kotlin, written and never compiled** (`apps/android`, build order step 6). Platform Views, **no Jetpack Compose, no AndroidX, and no dependency of any kind inside the APK** — `android.app.Activity`, the platform view classes, `HttpURLConnection` and the Kotlin standard library; `minSdk` 24 (Android 7.0), `targetSdk` 35. `apps/android/query` is a **third** implementation of `schema/query-spec.md`, held to the same `schema/fixtures` as `packages/query` and Swift's `DetroitQuery`. Two things are written out by hand rather than pulled in as a dependency, and each was checked against the real thing: the Detroit wall-clock rule (`Time.kt`) against the JVM's own tz database, 1990–2035 in 37-minute steps — 639,672 instants, no mismatch — and Ed25519 verification (`Ed25519.kt`) against RFC 8032's vectors, against tampered messages and signatures, and against the real `data/bundle/v1/index.json.sig`. **Nothing here has been compiled:** this Mac has no JDK, Kotlin, Gradle or Android SDK, and installing them is a download Kyle must OK. That is exactly where the iPhone screens stood on 2026-09-18. | The reader we design for is a cheap old phone on a bad signal; Compose alone would cost more than this whole APK. This does not supersede "Android is the web app" (2026-09-19): the PWA stays the Android answer until the Kotlin app compiles, runs and is checked on a real phone. |
| 2026-09-20 | Open | **JUnit 4 in `apps/android` is Eclipse Public Licence 1.0 — non-permissive, and test-only. For Kyle.** CLAUDE.md says to ask before adding a dependency with a non-permissive licence. It is never distributed: no phone ever sees it, and `:query:runFixtures` and the plain `kotlinc` route both run the whole fixture suite without it, so dropping it costs only the `:app` unit tests. | Flagged on purpose rather than waved through because it is "only a test dependency". |
| 2026-09-20 | Accepted | **"Open 24 hrs" printed for all seven days is entered as always-open.** DMC keeps two pages per hospital, and only the "… - Emergency" page prints "Monday: Open 24 hrs … Sunday: Open 24 hrs"; the hospital page prints *visiting* hours (8 a.m.–8 p.m.) under a heading that just says "Hours". The ER pages are the `source_url`, and the visiting hours are never used. | The 2026-09-18 rule lets only an unambiguous "24 hours" become open-now. Seven printed days of "Open 24 hrs" is that; taking a visiting-hours block instead would tell somebody the emergency room shuts at eight. |
| 2026-09-20 | Accepted | **Rows whose own site refuses scripts were read in a browser on 2026-09-20 and set `status: active`, `entry_method: web`** — **the reading was done by an AI agent driving a browser, and Kyle approved the results as steward** — including the detroitmi.gov rows, which **Kyle passed the human check for himself, in his own Chrome**, and then asked for that session to be used, one page at a time. **No agent clicked a challenge, submitted a form, accepted a cookie banner or signed into anything**, and no staff name, direct line or email was written down even where a page printed one. That closed all 38 rows `docs/CHECKS-2026-09-19.md` had to leave blocked — the shelter line (866-313-2520: **match**), 19 listing rows and all 18 Health Department programme rows — and produced 24 `health.dhd` listings and one `transport` listing. Write-up: `docs/research/2026-09-20/dhd-and-hand-checks.md`. | Extends the 2026-09-18 `entry_method: web` rule. CLAUDE.md: identify our requests honestly and, when a site blocks them, check by hand rather than disguising the request. A person clearing their own browser's challenge is a person checking by hand; a machine clearing it would not be. |
| 2026-09-20 | Superseded the same day (Kyle had the number added; see "Kyle's five answers") | **Children's Hospital of Michigan's emergency room is listed with no phone number.** DMC prints it nowhere as digits — only "(313) 745-KIDS", on the location list, the hospital page and the ER page alike. Converting the letters would be our arithmetic, not their published number, so the row carries the address and a notice saying why, and shows no Call button. | Never publish a number its owner has not published. One call to 745-KIDS, written down by a steward, turns this into an ordinary listing. |
| 2026-09-20 | Accepted | **When the programme owner misprints a number and the host's own page prints it correctly, the host's page is the source — and the number is labelled as the host's.** The City's page gives the Neighborhood Wellness Center at The Open Door Church of God in Christ a phone of "(313) 526-34205" — nine digits after the area code. No digit was guessed, dropped or rearranged. The listing instead cites the church's own page (odcdet.org), which prints 313-526-3460 beside 12411 East Seven Mile Road and was matched by `check:sources`, and labels the number **"Church office"** so nobody is told it is the clinic's own line; the hours stay as the City prints them. `data/seed/incoming/2026-09-20-open-door.txt`. DHD should be told about the misprint and asked whether the centre has a line of its own (`docs/CHECKS-2026-09-20.md`). | A misprint is not ours to repair: repairing it means inventing a number, and the nearest plausible number is how people reach the wrong stranger. A host publishing its own phone beside its own street is the same footing as any other owner's page. |
| 2026-09-20 | Open | **The John D. Dingell VA emergency room stays `proposed`, and only a person can release it.** va.gov prints every number through a `<va-telephone>` custom element that never renders for us, so the digits exist only as `contact=` attributes in the page's markup (Emergency Department 313-576-4436; main line 313-576-1000). That is the owner publishing them — but `check:sources` looks for the number as text and will never match it in any form we would write, so **no automatic check can ever confirm this row.** A steward confirms it by phone and sets it by hand. The address (4646 John R Street) and the VA's own "open 24/7 including holidays" are plain text and are not in doubt. | Our entry rule is a machine match against the owner's page. A row that can never pass it must not be allowed to pass it quietly. |
| 2026-09-20 | Open | **Four waiting decisions stay open, and Kyle has put all four after the hackathon submission:** the call to DWIHN about treatment for uninsured residents; the legal advice before condition-report photos are switched on; the review of the overdose steps by DHD, MDHHS or counsel; and a native Spanish speaker's review of `strings/es.json`. Each has its own row above; this one records that Kyle looked at all four on 2026-09-20 and chose the timing. | Kyle, 2026-09-20. None of the four blocks the hackathon; all four block a public release, and the photos one blocks a feature that is already switched off. |
| 2026-09-20 | Superseded the same day by the row "Safe streets is built from SEMCOG's crash layer": Kyle read this and said to add it anyway and flag it | **SEMCOG crash data was not ingested, and the reason is not only the terms. For Kyle.** Kyle said on 2026-09-20 to get the data in and flag it for review. Checking it again the same day found that SEMCOG's `crash2024_10year` layer does answer anonymously (1,335,379 rows), but its portal publishes **no terms at all** for those layers and the records are Michigan State Police CJIC's, not SEMCOG's own — so it was left out rather than ingested-and-flagged the way the transport layers were. Kyle's choices: ask SEMCOG (the email about attribution and indemnification has been drafted since 2026-09-19), ask MSP, or leave crashes unbuilt. Crashes stay unbuilt meanwhile; the City's own layer still holds 2011 only. | Every transport layer we did ingest is a public agency's own open data, which is the bar CLAUDE.md sets. Redistributing a third body's records under no stated terms is a different thing, and worth one question before it becomes a habit. |
| 2026-09-20 | Open (after the hackathon submission, Kyle 2026-09-20) | **Henry Ford-GoHealth Urgent Care in Dearborn Heights (26763 Ford Rd) is inside the bounding box and outside the four cities. For Kyle.** It is the nearest GoHealth centre, and Henry Ford's own sitemap puts none of the fifteen in Detroit, Dearborn, Hamtramck or Highland Park. The service area names four cities (Kyle, 2026-09-19), so it is not listed. Include it, or keep the named-cities rule? | The bbox is a sanity check on coordinates, not the service area. A rule bent once without deciding to stops being a rule, and the next suburb is a harder call than this one. |
| 2026-09-20 | Superseded the same day (by the row below: both are built, machine-drafted, and still Open until a native speaker has read them) | **Arabic and Bengali are planned. Not started, and not to be described as done.** Kyle asked on 2026-09-20 that they wait until everything else is finished. When they happen, each file will be machine-drafted from `strings/en.json` and **each will need a native reviewer before it ships**, the same condition Spanish carries; what a place wrote about itself is never machine-translated, in any language. Groundwork already laid: `arabic` and `bengali` are language flags in docs/03, everything in the laptop layout uses logical properties (`inset-inline`, `padding-inline`, `text-align:start`), and phone numbers are LTR-locked `<bdi>`, so right-to-left needs no second stylesheet. | Kyle, 2026-09-20. Saying the app speaks a language before a speaker of it has read a word would be the same kind of lie as saying "verified" for a row nobody checked. |
| 2026-09-20 | Open until a native speaker of each has read them | **Arabic (`ar`) and Bengali (`bn`) are built, machine-drafted, and not yet checked by a native speaker.** Supersedes the row above that said they were planned and not started: Kyle asked for both on 2026-09-20 once the rest was finished. `strings/ar.json` and `strings/bn.json` carry all 825 keys with the same placeholders and the same emergency numbers, and both were drafted by Claude — **no human speaker of either language has read a word of them**. The crisis, overdose, domestic-violence and sexual-assault screens are the first things a reviewer should read, then the freshness badges. Nothing in the app says the translations were checked, and `detail.in_english` still tells every non-English reader that a place's own words are that place's, untranslated. | The same condition Spanish carries (2026-09-18). Saying the app speaks a language before a speaker of it has read it would be the same kind of lie as "verified" for a row nobody checked. |
| 2026-09-20 | Accepted | **Arabic and Bengali use Western digits (0-9) everywhere, not Arabic-Indic or Bengali digits.** In the strings files, and in every date, time and number the apps format: the web asks `Intl` for `ar-u-nu-latn` / `bn-u-nu-latn`, iOS for `ar@numbers=latn` / `bn@numbers=latn`, Android for `Locale.forLanguageTag("ar-u-nu-latn")`. Tests in all three clients fail if a native digit appears in any value. | A phone number has to match the keypad the person dials on, a time has to match the sign on the door, and a Detroit street number has to match the street. Detroit's Arabic and Bengali readers read Western digits every day; a beautifully rendered ٩١١ that cannot be dialled helps nobody. |
| 2026-09-20 | Accepted | **Arabic is Modern Standard Arabic in a plain, everyday register — no bureaucratic vocabulary — and counted nouns are written so they stay correct for any number.** Detroit, Dearborn and Hamtramck readers come from Yemen, Iraq, Lebanon and Syria; no one dialect would read as home to all of them, and MSA does. Because the string layer is a plain `{n}` substitution with no plural rules, and Arabic has six plural forms, sentences are phrased to be right whatever the number (`قبل {days} يوم`, `عدد الأماكن: {count}`) rather than agreeing with one case and being wrong in five. Organisation and programme names, street addresses, 911/988/211, Narcan (glossed once as نالوكسون), WIC, SNAP, Bridge Card, DDOT, SMART, QLINE and MoGo stay as published. | A resident reads the words on the door and on the card in their hand; translating a programme's name makes the app and the door disagree. Fake plural agreement is a small lie repeated on every listing. |
| 2026-09-20 | Accepted | **The language switch shows all four languages, each in its own name (English · Español · العربية · বাংলা), and those names live in code, not in the strings files.** A language's own name has to be readable from inside every other language, so it cannot sit in a per-language file; `LANGS` in `apps/web/src/i18n.ts` holds them, each rendered with its own `lang` attribute. `lang.switch` changed meaning with it — it is now the word "Language" in each file, used as the switch's accessible name. The first language on a first run comes from `navigator.languages` in the phone's own order; the choice is kept on the device and never sent. | A two-language toggle cannot name four languages, and a person who reads only Bengali has to be able to find Bengali on an Arabic screen. |
| 2026-09-20 | Accepted | **`#/map` now parses.** The Map tab writes `#/map` into the URL, but `fromHash()` in `apps/web/src/router.ts` never listed `map`, so a shared or bookmarked map link opened Home and Back from the Map tab skipped it. Found during the keyboard walk of the accessibility audit; fixed, with a round-trip test for every tab id. | A link a person sends to someone who needs it must open what they meant to send. |
| 2026-09-20 | Superseded the same day by the row "SEMCOG's terms were described wrongly" below: the acceptance recorded here was made against a wrong description of SEMCOG's terms | **"Safe streets" is built from SEMCOG's crash layer, on Kyle's instruction, and flagged for review.** Kyle read the row above — that the layer answers anonymously, that SEMCOG's portal publishes no terms for it, and that the records underneath are the Michigan State Police's (CJIC) — and said to add it anyway for the hackathon and flag it. Built as `pipeline/src/ingest-crashes.ts` (`pnpm ingest:crashes`, run by hand): crashes with a person walking or biking, in the four cities, 2020 to 2024 (the five most recent complete years; the layer ends at 2024), aggregated at ingest to **counts per neighborhood per year** by mode and by whether someone was killed or seriously hurt. Everything else is dropped as it is read — no crash id, no date or time beyond the year, no ages, no driver, vehicle, alcohol or hit-and-run fields, and no coordinates in the output. Counts under 5 are stored as `lt5`. `data/ingested/crashes.json` is committed with the source name, URL, date read, years and `license: unstated`. The bundle carries the five-year totals only; the neighborhood page shows them as plain counts with the years on screen, beside the whole-city number, with no rate (docs/13 defines no denominator), no ranking, no colour that reads as a score and nothing on the Map tab. Read 2026-09-20: 3,043 crashes in the four cities, 2,486 of them inside a Detroit neighborhood outline; Detroit 2020–2024, 2,024 crashes with someone walking, 664 with someone biking, 630 in which someone was killed or seriously hurt. | Kyle, 2026-09-20, knowing the facts above. Supersedes the 2026-09-18, 2026-09-19 and 2026-09-20 rows that left crashes unbuilt. |
| 2026-09-20 | Open | **Ask SEMCOG and the Michigan State Police about terms and attribution for the crash layer, after the hackathon submission — and remove the layer if either objects.** The email to SEMCOG (infocenter@semcog.org) about attribution wording and the indemnification clause has been drafted since 2026-09-19; MSP owns the underlying reports and has not been asked at all. Until an answer comes, the licence stays recorded as `unstated` in `data/ingested/crashes.json`, the panel names SEMCOG and says the reports are the State Police's, and `pnpm ingest:crashes` is run by hand rather than on a schedule. **[Corrected 2026-09-20: SEMCOG's terms are not unstated — see the row "SEMCOG's terms were described wrongly" below. The question to SEMCOG is now specifically about the indemnification clause, and `license: unstated` in `data/ingested/crashes.json` is itself wrong and is for the data owner to fix.]** | The follow-up half of the row above. Redistributing a third body's records under no stated terms is worth one question; Kyle chose to ask it after the submission rather than before. |
| 2026-09-20 | Accepted | **Kyle, as steward, approved publishing what each organisation's own website says** for the differences listed in `docs/CHECKS-2026-09-20.md` section 1, which is the steward approval CLAUDE.md requires before any phone, address or coordinate change. Applied to `data/seed/resources.csv` and `data/seed/schedules.csv` only: Team Wellness (`sal_team_east_clinic` re-described as Team Jefferson, 11105 E Jefferson Ave., (313) 332-0257, Mon-Fri 9-5, because 6309 Mack is already live as the East Detroit Crisis Stabilization Unit and a door is not listed twice), St Raymond (20103 Joann), Brightmoor (22735 Fenkell Ave., `source_url` moved to the pantry page), Wayne Metro East and West (appointment only, walk-in schedules dropped), Capuchin Services Center (Mon-Fri 8:30-4:00, first visit by appointment), Scott Memorial (ZIP eligibility confirmed), Lifeline H2O (enrollment full, dated), the Housing Resource HelpLine (its published hours), and one new listing, Capuchin's shower program at Meldrum. **Contradictions between an owner's own pages are never resolved by us: the row goes call-first with the hours shown as written and a notice to call.** That covers Wayne Metro's walk-in days (Monday-Thursday on the location directory, Monday-Friday on the home page) and Cherry Hill Helping Hand (picture ID or none, monthly or every other month), and both stay on the call list in section 3 of the worksheet. | Publishing an owner's own words is the only claim we can honestly make when nobody has phoned; picking a side in an owner's own contradiction would be inventing the fact we most need to be right. |
| 2026-09-20 | Accepted | **Two map contrast items stay below WCAG's 3:1 on purpose: minor residential streets (about 1.45:1) and the ground outside the four cities (1.20:1).** Main roads, freeways and the city boundary were raised past 3:1 instead, and every map has a full text list. (docs/ACCESSIBILITY-AUDIT-2026-09-20.md) | Kyle, 2026-09-20: raising them turns the map into a grey slab and buries the greenway, the layers and the listing dots; the streets that carry meaning pass. |
| 2026-09-20 | Accepted | **Kyle approved installing a JDK and Gradle (Homebrew formulae, no password, no licence click-through); the Android SDK is not installed, because accepting its licence agreements is Kyle's own act.** `apps/android` therefore has three modules: `:query` and `:core` (the android-free files of the app, same files not copies) compile and pass on a plain JDK and in CI; `:app` joins the build only when an SDK is found. `Reports.kt` and `Saved.kt` were split so their privacy rules sit in `:core` and run on every build. | The first compile found three errors, one bug (a `/*` inside a KDoc comment); the screens in `:app` have still never been compiled. |
| 2026-09-20 | Accepted | **Every listing that already offers bus directions also offers them in the Transit app, as a second link under the first.** Transit (transitapp.com) is the app DDOT and SMART riders use for real-time buses. We use **Transit's own documented URL scheme**, `transit://directions?from=…&to=…` from <https://transitapp.com/developers>, and send **only `to`** — their own note says a missing parameter uses the person's current location, which the Transit app asks for itself, on their phone, so we pass no origin and read none. The destination is the publisher's coordinate when there is one and the written address otherwise (the reverse of our maps links, because Transit documents that it geocodes address strings with the person's location in mind); a coordinate is still never printed as an address. It is a **plain link and nothing else**: no SDK, no script, no preconnect, no icon or asset of theirs, nothing loaded from transitapp.com until the person taps. The existing "Bus directions" link, which needs no app, stays first and was not replaced. Sources in `docs/research/2026-09-20/transit-app.md`. | A person standing in a listing needs the bus that is actually coming, and this is the app Detroit riders already have. Using the format its owner publishes, with the destination and nothing else, adds a way to get there without adding anything to what leaves the phone. |
| 2026-09-20 | Accepted | **The Transit link is never shown where directions are withheld, and never on a laptop.** It sits inside exactly the gate `transitHref` already used — an address or the publisher's coordinate — so DV and mental-health-crisis listings, which carry neither, show no Directions, no Bus directions and no Transit link; treatment and sexual-assault listings keep all three, because people have to get there. And because Transit documents **no https universal link and no behaviour when the app is not installed**, and ships for iOS and Android only, the web hides the link off a phone user-agent (the same test `directionsHref` uses) and iOS hides it unless the app is installed (`LSApplicationQueriesSchemes`, a local question that sends nothing). | One sensitivity rule, not two. And a link that can only fail is worse than no link: we would rather show nothing than guess a URL Transit has not published. |
| 2026-09-20 | Accepted | **The Transit app is a link-out, not an endorsement, a recommendation or a partnership, and the app never says otherwise.** It appears in the Map tab's trip planners ("Transit app: live DDOT and SMART buses on your phone") and leads the "Rides to the doctor or cheaper bus fare" link-outs, in the same owner-named, leaves-the-app format as every other link-out. The words say a company called Transit makes it, that we do not run it and neither does the bus company, and that **SMART's own page** lists it among third-party apps that receive SMART data (<https://www.smartbus.org/Ride-SMART-Bus-App>, read 2026-09-20). **Nothing says DDOT recommends it**: DDOT's pages are on detroitmi.gov, which returned 403 to our request, and per CLAUDE.md we did not work around that — so we have no DDOT page and make no DDOT claim. A steward who wants that sentence must open the DDOT page by hand. | We state only what an owner page states. Borrowing a transit agency's authority for a company's app would be the same kind of lie as "verified" for a row nobody checked. |
| 2026-09-20 | Open | **Ask Transit whether they want anything for the link — attribution wording, a name or logo rule, or nothing at all.** Their developer page invites developers to use the scheme ("you can use our URL scheme") and states **no terms, no branding rules and no permission requirement**. Silent is not the same as permissive, so until someone asks (partners@transit.app is the address on their own page) we use the plain word "Transit" in text only, no logo, no icon, no asset of theirs, and nothing fetched from their servers. | The same posture as every other unstated licence in this project: use the least we can, name the owner, and put the question in writing. |
| 2026-09-20 | Accepted | **Kyle authorised accepting the Android SDK licence on his behalf** (asked in chat while he was at work). Scope: the command-line tools and the licences for `platform-tools`, `platforms;android-35` and `build-tools;35.0.0`. One licence was accepted, `android-sdk-license`; the other six that `sdkmanager` offers were declined as unneeded. CI does not accept a licence: the `android-query` job sets `HELP313_NO_ANDROID=1` and builds `:query` and `:core` only. | It unblocked the first compile, tests, debug build and emulator run of `apps/android/app`; the agreement was Kyle's to give, and a workflow should not give it for the repository. |
| 2026-09-20 | Open | **The Android app has never run on anything below Android 15.** The first compile found a call that binds to a method that exists only from API 35 (`List.getLast()`), which would have crashed every phone from minSdk 24 to Android 14; it is fixed and lint reports no other case, but only an API 35 emulator has run the app. Before any release: run it on an old, cheap phone (and measure the cold start, where the hand-written Ed25519 check took about 20 seconds on the emulator). | The phones the app is for are exactly the ones nobody has tried. |
| 2026-09-20 | Accepted | **`pnpm import:lines` reports an `sal_id` collision instead of dropping the line.** A line whose slug already exists is skipped with `skipped: id collision with <sal_id>` and counted in the summary; a line that corrects an address for the same organisation and the same phone prints an informational note rather than a skip. Ids themselves are unchanged, and nothing is ever overwritten by an import. | Found by CHECKS-2026-09-20 §1.14: a 25-line backlog file produced 24 listings and the importer said nothing about the 25th, so the only way to notice was to count. A tool that loses data silently is worse than one that refuses. |
| 2026-09-20 | Accepted | **"am" and "pm" are translated strings (`clock.am`, `clock.pm`) while the digits beside them stay Western, and the list separator is a string (`list.sep`).** All three are new keys in all four strings files and all three join the native-review queue; the Bengali draft is the transliteration এএম / পিএম, which a reviewer may replace. The iPhone (`HelpApp/Help.swift`) and Android (`Format.kt`) apps still hard-code am/pm and should read the strings instead. | A time on a screen should read in the reader's own words; the digits have to match the sign on the door (DECISIONS 2026-09-20, Western digits). |
| 2026-09-20 | Accepted | **A dollar amount is written the way the record writes it (`$85,000`), falling back to `en-US` formatting when a language's own `Intl` rules do not put the sign first.** ICU renders USD in `ar` and `bn` in forms a Detroit reader would not recognise on a sale record. | A home-sale figure on a neighborhood page is a quoted public record, and it has to look like the record. |
| 2026-09-20 | Accepted | **Uppercase and letter-spaced section headings apply only under `:lang(en)` and `:lang(es)`, and the map's pan pad is laid out with `[dir="rtl"] .mappan { flex-direction:row-reverse; }` rather than a direction override.** Letter-spacing breaks Arabic joining and Bengali conjuncts; the pad keeps west on the left, because the map never mirrors. Also from the same walk-through: the owner-written heading in the top bar is marked `lang="en"`, and a phone number and its extension sit inside one outer `<bdi class="tel">`. | Found by walking every screen in Arabic and Bengali in a browser. Typography that is merely stylish in English destroys letterforms in two of our four languages. |
| 2026-09-20 | Open | **Searching in Arabic or Bengali returns nothing, and the empty state does not say why.** Every listing is written in English — names, streets, organisations — so a query typed in either script cannot match. The honest fix is one plain line on the empty results screen saying places are listed in English and offering the categories, and **its wording waits on the native reviewer** rather than being guessed at (CHECKS-2026-09-20 §7). Also open and cosmetic: printing an Arabic screen puts a bare left-to-right URL in parentheses inside right-to-left text. | Writing that sentence ourselves would be the same mistake as shipping an unreviewed translation: the person who reads the language decides how to say it. |
| 2026-09-20 | Accepted | **Android: a "verified once" cache for the bundle was rejected.** The copy in app storage is checked against a pinned key on every read, so a forged `index.json` is refused today; remembering a hash would make it accepted. Cold start was fixed instead by showing Urgent help before the check, a baseline profile (the first run of the signature code was being interpreted, not compiled: about 4 s in a debuggable build, about 0.3 s in the shipping shape), and faster field arithmetic (38 ms to 24 ms warm). Android has no software Ed25519 provider even on API 35, so the hand-written check runs on every phone. | CLAUDE.md says refuse a mis-signed bundle, not refuse it the first time. All timings are from an emulator; an old phone is still unmeasured (Open row above). JSON parsing (0.8 to 4 s) is now the largest cost. |
| 2026-09-20 | Resolved 2026-09-20 (Kyle: "SEMCOG is fine for now"; see the row "Kyle's five answers after the adversarial review") | **SEMCOG's terms were described wrongly, and the acceptance above was given against that wrong description.** Three rows of 2026-09-19 and 2026-09-20, `data/sources.yaml`-adjacent notes, NOTICE, README, docs/02, docs/13 and OPERATIONS all said SEMCOG "publishes no terms" for the crash layer, or recorded it as `unstated`. That is false, and it was already known to be false in this repository: `docs/research/2026-09-19-wayne-county-sources.md`, written the day before, records SEMCOG's portal-wide **Copyright License Agreement** (<https://maps-semcog.opendata.arcgis.com/pages/copyright-license-agreement>), which grants a perpetual, royalty-free licence to reproduce, modify and publish, **requires** the notice "Copyright © \<year\> SEMCOG. All Rights Reserved. Reproduction or Use Without Permission is Prohibited.", contains a **one-way indemnification clause** under which the user indemnifies SEMCOG, and grants **no third-party rights** — which matters because the records underneath are the Michigan State Police's (CJIC), who have not been asked. Corrected in README, NOTICE, docs/02, docs/13, docs/09 and OPERATIONS on 2026-09-20; the required notice is now carried in NOTICE, and printing it on the Safe streets panel is an open item (`hood.crash_source` names SEMCOG and the State Police but not the notice). **What is open is not a wording question, it is Kyle's:** accept the Copyright License Agreement *including* the indemnification clause, knowingly, or remove the layer (delete `data/ingested/crashes.json` and rebuild; the panel disappears on its own). | An acceptance recorded against a wrong description of the terms is not an acceptance. This one also mattered in the other direction: the agreement is more permissive than we said, and it obliges us to print a notice we were not printing. Supersedes the "Safe streets is built from SEMCOG's crash layer" row above, and narrows the Open row about asking SEMCOG. |
| 2026-09-20 | Resolved 2026-09-20 (Kyle: keep it out of everything except the docs; see "Kyle's five answers") | **Record the DHD conversation, or keep the claim off public pages.** Four documents said "DHD program staff are aware of this project and informally supportive of reusing the public information from the D Compassion build" (README, docs/README, docs/02, docs/07). **There is no record of that conversation anywhere in this repository** — no date, no substance, no note of who said what — and a claim of a public health department's informal support is exactly the kind of claim a reader would weigh. It has not been deleted, because it may well be true and it is Kyle's to state: the public README now retreats to what is documented (the app rebuilds the intent of D Compassion from public program information; DHD has not authorized it and maintains no data for it; nothing is endorsed by the City), docs/README keeps the sentence marked "(Kyle's account; no written record in this repository)", and docs/02 and docs/07 carry the same mark. **For Kyle:** write down the date and substance of the conversation, or leave the sentence out of public pages. | Everything else in this project is held to "name the page and date it." A claim about a person or an institution cannot be held to a lower bar than a claim about a food pantry's hours. |
| 2026-09-20 | Resolved 2026-09-20 (built; see "Kyle's five answers") | **Recommend one line on the About screen saying this is an independent project.** Not a reversal of Kyle's 2026-09-20 decision to remove "Not an official City of Detroit app" — that decision stands until he changes it. What changed since is the app's contents: it now draws and names **DDOT**, **SMART**, **Detroit Health Department** programs and **SEMCOG/Michigan State Police** layers, and there is no independence statement anywhere a resident can see. The old line's other job was the store listing, and **no store listing exists yet**, so today nothing tells a resident whose app this is. Recommended wording, one line, on About only: "An independent project. Not from the City of Detroit, DDOT, SMART or the Health Department." In the README roadmap under **Now**, waiting on Kyle. | The reason the line was there in the first place (docs/08: App Store review, and keeping this app apart from the City's own Detroit Compassion App) got stronger, not weaker, when agency-named layers went in. Recorded as a recommendation so that the decision, either way, is a decision. |
| 2026-09-20 | Open | **Wayne County's station map is read through Google's My Maps KML endpoint, and we should ask the County for the file instead.** `pipeline/src/ingest-mymap.ts` reads `https://www.google.com/maps/d/kml?mid=…&forcekml=1`. The **content is the County's** and is named as the County's on every row; the **delivery is Google's**, and that endpoint is not a documented API — it can change or stop without notice, and Google states no terms for our using it. Nothing is written back and the request identifies itself honestly, as every ingest does. **Ask the County for the underlying file (a CSV, a KMZ, or a feed) in the same message that asks about reuse** (the Open row above). Until then the ingest stays by hand and the committed CSV is what a steward diffs. | Reading a public body's own facts is the bar CLAUDE.md sets; reaching them through a third party's undocumented endpoint is a dependency we did not choose and did not write down. |
| 2026-09-20 | Accepted | **NARCAN's trademark is acknowledged in NOTICE, and the app keeps using the word.** NARCAN is a registered trademark of Emergent BioSolutions Inc.; naloxone is the drug. The app says "Narcan" because that is the word residents, outreach workers, the Health Department's own station list and Wayne County's own map use, and a person looking for it will not search "naloxone". NOTICE now states the trademark, that we use it as the word residents use, and that there is no affiliation with, sponsorship by or endorsement from Emergent BioSolutions. Category labels and listing text are unchanged. | Plain language beats pharmacological precision on a screen somebody reads in a hurry (CLAUDE.md: "Free groceries," not "Food pantry services"). Acknowledging the mark costs nothing and is the honest half of using it. |
| 2026-09-20 | Accepted | **Two emergency-room rows at 22101 Moross Rd are deliberate, not a duplicate.** `sal_henry_ford_emergency_room_at_st_john` and `sal_henry_ford_children_s_emergency_room_at_st_john` share an address and a switchboard (313-343-4000) and have **two different owner pages** — `henryford.com/locations/st-john-hospital` and `…/st-john-childrens-hospital` — because they are two emergency rooms: one for adults, one for babies, children and teens. A parent driving at 2am needs to know the children's ER is there; collapsing them into one row would hide the fact somebody came for. The same reasoning is why DMC's adult and children's ERs are separate rows. | A rule against two rows at one address would be a rule about buildings, and an emergency room is not a building. Recorded so a future de-duplication pass does not "fix" it. |
| 2026-09-20 | Superseded by the row below (same day) | **The shared query rules have no notion of a holiday, so the app will say "Open" on Christmas Day.** Checked against the current bundle on 2026-09-20: at noon on Friday 25 December 2026, **101 of the 184 listings that carry a schedule report `open`** — the same 101 as the Friday before, because nothing in the data or in `packages/query/src/schedule.ts` marks a closed day. Thanksgiving noon: 115. Nothing is lying on purpose — an RRULE says "every Friday" and the pantry's own page says "every Friday" — but "Open" on Christmas Day is the app sending somebody to a locked door, which is the one failure this project exists to prevent. Options, in order of how honest they are: a holiday list in the bundle that turns those rows to closed with a reason; the same list shown as a warning on every schedule near a holiday; or, weakest, a banner. Whatever is built, **`schema/fixtures/` gets the cases first** and all three implementations (TypeScript, Swift, Kotlin) must agree, and a holiday a place chooses to open on must be expressible. In the README roadmap under **Now**. | Every other freshness rule in this project is about a fact going stale. This one is about a fact that was never true on one day of the year, and 101 rows is not an edge case. |
| 2026-09-20 | Accepted | **A holiday makes a schedule-derived "open" into a distinct `holiday` status, computed on the device from a date rule.** `openNow` on an `active`, `scheduled` row whose window opens on a holiday — or whose only remaining time today is on one — answers `holiday`, carrying the schedule's own times as **usual hours** and never `open`, `closes_at` or `minutes_left`; it ranks exactly where `call_first` ranks, so it can never sort above a row that is known to be open. The rule is the **eleven United States federal holidays** (New Year's Day; Martin Luther King Jr. Day, 3rd Mon Jan; Washington's Birthday, 3rd Mon Feb; Memorial Day, last Mon May; Juneteenth; Independence Day; Labor Day, 1st Mon Sep; Columbus / Indigenous Peoples' Day, 2nd Mon Oct; Veterans Day; Thanksgiving, 4th Thu Nov; Christmas Day) **plus the observed day** when a fixed-date one falls at a weekend (Sat → the Friday before, Sun → the Monday after; both days count). **The day after Thanksgiving and Christmas Eve are deliberately out**, and so is every other day some people take off: Detroit is largely open on them, and a wrong "holiday" costs a person hours we do know. The rule follows the day a window **opens**, so an overnight warming centre that opened on Christmas Eve stays `open` into Christmas morning. A steward can turn the whole rule off for one row with the existing `flags` value **`open_holidays`**, set only when the owner's page says so — an ordinary flag, so it rides from `data/seed/resources.csv` through the bundle with no new field. Occurrences on a holiday are **labelled** in "Next times", never dropped. No list of dates is in the bundle, so a phone with a three-month-old copy still knows what Christmas is. Spec: `schema/query-spec.md` "Holidays"; 40 cases in `schema/fixtures/12-holidays.json` (years 2026–2030, both observed shifts, the Monday rules, an overnight window on the eve, `always` and `call_first` untouched, the flag), all passing in TypeScript, Swift and Kotlin. Measured on the bundle of 2026-09-20: at noon on 25 December 2026 **100 of 183 scheduled rows said `open` before this and 0 do now** (104 say `holiday`); Thanksgiving 113 → 0; New Year's Day 2027 100 → 0; observed Friday 18 June 2027 101 → 0; and Saturday 26 December 2026, an ordinary day, is 39 before and after. | "Unknown is never rendered as open" (docs/01) is the whole point, and on a holiday a schedule-derived "open" is not knowledge — nobody's holiday hours are in any source we read. A computed rule beats a data file because it cannot go stale on a phone, and the federal list is the only one that is both short and almost always right; anything wider would hide real hours, which is the opposite failure and just as harmful. |
| 2026-09-20 | Accepted | **The local steward login stops trusting the `Host` header.** `DEV_STEWARD` is now honoured only when its value is **exactly `local`** *and* **neither `ACCESS_TEAM_DOMAIN` nor `ACCESS_AUD` is configured**; the request's `Host` is no longer part of the decision, and the steward it stands in for is recorded as `dev:local`. Supersedes the 2026-09-18 row that honoured it "only when the request host is `localhost` or `127.0.0.1`". docs/OPERATIONS and DEPLOY-HANDOFF updated. | `Host` is a header any caller can set, so a rule about look-alike hostnames was a rule about a string an attacker controls. Two things nobody outside our own machine can set — an exact variable value, and the absence of Access configuration — are a real lock, and filling in the Access fields now switches the dev login off by itself. |
| 2026-09-20 | Accepted | **The Worker's `onError` and `notFound` handlers log nothing at all.** Hono's defaults call `console.error`, which would have written a D1 error's statement **and its bound values** — a target id, a per-day dedupe hash (`client_nonce`), a steward's note, a masked detail — into Workers Logs, where they are kept for days and readable by anyone who can open the dashboard. Found and proved by a failing test, not by reading. The request path now logs nothing whatever, not even the route pattern, because Workers Logs stamps its own second-precision time and that alone would make an error into a "somebody was on this screen at 17:41:37" record. | CLAUDE.md: never log or persist client IPs, and timestamps at minute granularity. An error handler is the one place in a Worker where a promise about what is never stored is easiest to break by accident, because nobody wrote the logging line on purpose. |
| 2026-09-20 | Accepted | **Workers Logs are off by configuration, and one allow-listed logger is the only `console` call in the Worker.** `api/wrangler.toml` sets `[observability.logs] enabled = false` alongside `invocation_logs = false`, because Cloudflare's own documentation says invocation logs capture "request metadata, and headers" (<https://developers.cloudflare.com/workers/observability/logs/workers-logs/>) — which is the thing this project promises never to keep. The only permitted `console` call is `api/src/log.ts`, whose `FixedMessage` type is an allow-list of two fixed sentences about the nightly cron ("the nightly photo pass failed", "the nightly retention pass failed"), so a caller cannot smuggle a message through it; a test enforces that it is the only one in the Worker. Two things a config file cannot prove are now steps in DEPLOY-HANDOFF for after the first deploy: confirm Observability → Logs is off for the Worker, and confirm no account-level Logpush job for Workers Trace Events exists. And a standing rule: **never paste `wrangler tail` output anywhere** — it streams live requests, URLs and headers. | `invocation_logs = false` was already set, but the docs do not say whether an uncaught exception still lands in that same log, and an exception message from D1 can quote bound values. Turning the whole feature off is the only setting whose meaning does not depend on reading Cloudflare's docs correctly. |
| 2026-09-20 | Accepted | **Three steward endpoints moved onto closed schemas with size caps**, like the public write endpoints already were: an unknown field is a 400 and an over-long field is refused, rather than being accepted and stored. | DECISIONS 2026-09-18 made request bodies closed schemas so a client bug fails loudly in development. The steward routes were behind Cloudflare Access and had been left out; "behind a login" is not a reason to accept a field nobody designed. |
| 2026-09-20 | Accepted | **`entry_method: auto_check` is a claim the build checks, against `data/seed/script-refusing-hosts.csv`.** An `active` row may say a script matched its own page only if it has a `source.url` and a `checked_at_entry`, and only if that host is not recorded as refusing scripts on or before that date; otherwise `pipeline/src/validate.ts` raises an error and the build stops. The file holds three hosts today — `detroitmi.gov` and `dmc.org` (from 2026-09-20) and `cskdetroit.org` (from 2026-09-19), each with the refusal seen, the day it was checked and a note. **Adding a host when a site starts refusing us is a steward duty** (OPERATIONS). Separately, a page that answers 200 with fewer than 200 visible characters, or with a known challenge or block page, is now **unreadable** rather than a failed match (`pipeline/src/page-match.ts`). | The Children's Hospital ER row claimed `auto_check` while `dmc.org` had refused our fetcher all day: the badge said a program had matched a page no program could read. A badge is a statement about who looked, and the build is the only thing that can hold it to that. |
| 2026-09-20 | Accepted | **An aggregate publishes its window and not the cells inside it.** `data/ingested/crashes.json` carried both five-year totals and per-year counts, and **18 suppressed "fewer than 5" cells could be recovered exactly** by subtracting the published years from the total (13 in neighborhoods, 5 in the city figures). The per-year numbers are gone; only the 2020–2024 window is published, per neighborhood and per city, and a test proves more than one value fits every hidden cell. The file also now carries SEMCOG's licence, its licence URL and the required copyright notice (the year taken from the layer's own last-edited date). | Suppressing a small count and then publishing enough arithmetic to undo it is not suppression. docs/13's rule is that counts under five are hidden; this is what it takes for that to be true. |
| 2026-09-20 | Accepted | **Private listings cannot be shared, and private kinds are traceless at the list level too.** `canShare = !isPrivate` (`apps/web/src/saved.ts`), the same predicate as `canSave`, so there is one definition of private (`isPrivate` in `apps/web/src/needs.ts`: `treatment` and `assault`, plus the `shelter.dv` / `health.mental` pair) rather than one rule for saving and another for sharing. A private **category list** now writes no hash either — only a random history key and the plain path — and keeps the plain app name as the window title; an arriving private deep link is `replaceState`d out of the URL. | The 2026-09-18 rule covered a private listing's own screen and Saved; a Share button on that screen, and `#/c/treatment` in the address bar, both put the fact in someone else's hands anyway. A shared phone is the threat (audit A8) and it does not care which screen leaked. |
| 2026-09-20 | Accepted | **The iPhone keeps nothing where a backup or a cache would carry it off.** The install key, the outbox and saved places live in `Library/Application Support/state/`, which `DeviceState.prepare` marks `isExcludedFromBackup` — **re-applied on every launch**, because a restore or an OS upgrade can clear the flag — and the bundle snapshot moved to `Library/Caches/bundle`, which the system may reclaim and which no backup keeps. All network traffic goes through one ephemeral, cookie-less, cache-less `URLSession` with a fixed user agent (`313Help-iOS/<version>`) and no credential store. | docs/08 promises a random, resettable secret; a secret that survives a restore onto a new phone is neither. And a cached copy of a public list has no business in anybody's iCloud backup. |
| 2026-09-20 | Accepted | **A pinned key must be the right shape and must not be a small-order point, in the app and in the build.** 44 DER bytes with the Ed25519 prefix, canonical y, and a refusal of the known small-order public keys — written three times on purpose, in `apps/ios/Sources/HelpCore/Verify.swift`, in `apps/ios/Scripts/preflight.sh` (which fails a Release build) and in `apps/ios/Scripts/verify-snapshot.swift`, with tests reading the two scripts as text to hold all three together. Android refuses them at runtime by arithmetic (`[8]A == O`, `Ed25519.kt`) and again in the release gate, which reads the list out of the source file and checks its length. | A small-order key makes a signature check pass for anything, so a typo or a malicious pin would silently turn verification off — the one check everything else in this project rests on. |
| 2026-09-20 | Accepted | **A Release build of the iPhone app verifies the snapshot it ships and embeds a rollback floor.** `apps/ios/Scripts/verify-snapshot.swift` checks `index.json.sig` against that configuration's pinned keys and every file's SHA-256, then writes the snapshot's own `generated_at` into `bundle-floor.txt`, which the app reads (`Config.snapshotFloor`) and refuses to go below. **Known limit:** the build phases that run this and `preflight.sh` live in `apps/ios/Xcode/`, which is git-ignored, so the enforcement itself is not in version control — only the scripts and the README recipe that rebuilds the project are. Debug only warns. | A phone that shipped with a good list should never be talked back to an older one. The floor is the shipped snapshot rather than a hand-set number so it can never be forgotten; the git-ignored project is a real gap and is written down rather than glossed. |
| 2026-09-20 | Accepted | **`PrivacyInfo.xcprivacy` declares exactly one collected type, Other User Content**, not linked to the person and not used for tracking, with no tracking domains and no required-reason APIs. **The App Store nutrition label must match it**, and **Photos must be added to both if condition-report photos ever ship.** | A report's note and a proposal's text are content a person chose to send us; nothing else is collected. A manifest and a store label that disagree is the kind of thing review catches, and the honest fix is to keep one true. |
| 2026-09-20 | Accepted | **Privacy-critical native logic lives in `HelpCore` and `:core`, where CI can run it.** On iPhone, `apps/ios/Sources/HelpCore` now holds the install key and its daily hash, the outbox, the saved-places rules, the one session, the signed-bundle check and the release rules — 62 tests that used to run only inside the git-ignored Xcode project and so were checked by nothing. On Android, `:core` compiles **the app's own android-free files**, named one by one (`Net.kt`, `Http.kt`, `Outbox.kt`, `Route.kt` joined `Ed25519.kt`, `Verify.kt`, `ReportModel.kt`, `SavedRules.kt`), on a plain JDK. **Still outside CI's reach:** the Swift signature maths and the backup-flag test sit inside `#if canImport(CryptoKit)` / `#if canImport(Darwin)`, so on the Linux CI container they pass while asserting nothing. | A rule written inside a screen is a rule nothing checks. These are exactly the rules where a mistake is worst, and the fix is not more discipline but a target a workflow already runs. |
| 2026-09-20 | Accepted | **Tests assert behaviour, not the text of the source.** `apps/web/test/behaviour.test.ts` (36 tests) drives the thing itself — the full-screen overlay's `inert` set, focus restoration, the private/shareable predicates, the link-scheme allow-list, the `_headers` file parsed structurally, the overdose note, the two entry-check badges in all four languages — and a handful of source greps were retired in favour of it. **What is left is a standing job, not a finished one:** about 64 of the 114 tests in `apps/web/test/web.test.ts` still read a source file and assert on its text. Convert them as you touch them. | A grep for a source line passes when the line is moved, renamed or left dead, and fails on a harmless rewrite — it tests the shape of the code instead of what the code does. Counting `role="status"` in the source was the case that proved it: every live region in the app was unannounced and the test was green. |
| 2026-09-20 | Accepted | **Android: config changes survive in an in-process holder that is cut at the first private screen, and never in `savedInstanceState`.** `Route.Retained` keeps one volatile list of routes; `keepable()` truncates it at the first private screen, falling back to Home, so a private screen is never in it at all. `onSaveInstanceState` is deliberately not overridden, and most changes never recreate the activity: the manifest declares `fontScale`, `uiMode`, `locale`, `layoutDirection`, `density`, `orientation`, `screenSize` and the rest, and `onConfigurationChanged` simply redraws. `Retained.clear()` runs when the activity is finishing. | `savedInstanceState` is written into the system's own process-death record, which outlives the app and is exactly the wrong place for "this person was reading about treatment". Rotating a phone must not be a way to make that durable. |
| 2026-09-20 | Accepted | **Android: `FLAG_SECURE` is set per screen, not once for the app.** Every draw calls `keepOutOfScreenshots(Route.isPrivate(route))`, which adds the flag on a private screen and **clears** it again on an ordinary one. The trade-off is deliberate and worth stating: on an ordinary listing a person **can** screenshot an address to show somebody or to find it again, which is a thing people really do, and on a private screen they cannot — not by hand, and not by the recent-apps thumbnail. (iOS is the other way round: its shield covers the whole window whenever the scene is inactive, because that is what the platform offers; quick exit there is per screen.) | An app-wide flag would take a useful, ordinary action away from everyone to protect four screens. A per-screen flag protects the four and leaves the rest alone. |
| 2026-09-20 | Accepted | **Android: https only, one header, no cookies, no caches, no redirects — and no debug exception to any of it.** `Http.open` refuses a non-https URL, sets `313Help-Android/<versionName>` as the only request header, turns the process's default `CookieHandler` and `ResponseCache` off, and sets `instanceFollowRedirects = false` and `useCaches = false`. The manifest sets `usesCleartextTraffic="false"` and `allowBackup="false"`, and there is **no `src/debug` source set and no cleartext override in the debug build type**, so the debug build is held to the same rules; the Debug bundle base is an `https://…invalid` placeholder. Locks are never held across the network: `ReportStore`'s file lock covers disk work only and `post()` runs outside it, so a stalled send cannot block "Make a new key". Writes to the state files are atomic (temp file, `fd.sync()`, rename) and an outbox that cannot be parsed is renamed aside, never deleted, and the person is told (`privacy.queued_unreadable`). **Known gaps:** the bundle cache is *not* written atomically (`deleteRecursively` then `writeBytes`), and report kinds are filtered by the screen that offers the buttons rather than by the store or the parser. | A debug cleartext exception is how a development convenience becomes a shipped one; and a lock held across a network call is how a privacy control — the key reset — ends up waiting on somebody else's server. |
| 2026-09-20 | Accepted | **Android's Detroit wall-clock rule is correct from 1987 and clamps anything earlier to Eastern Standard Time.** `Time.kt` writes the US rule out by hand — 2007 onward (2nd Sunday in March to 1st Sunday in November) and 1987–2006 (1st Sunday in April to last Sunday in October) — because Android has no time-zone database below API 26 without a dependency. TypeScript and Swift ask the platform's own tzdb (`Intl.DateTimeFormat`, `TimeZone(identifier:)`) and are right for all of history. The three can differ only before 1987, which is why no shared fixture pins a pre-1987 instant; Android pins the clamp itself in `FixtureTest.theZoneRuleIsClampedBefore1987`. | The app reasons about this week's pantry hours. Writing the rule out avoids a dependency inside an APK that has none, and the limit is recorded in the spec rather than discovered later. |
| 2026-09-20 | Accepted | **The greenway tab on the iPhone is labelled `tab.map`.** It was `gw.title` ("Joe Louis Greenway") after `tab.rec` was retired (DECISIONS 2026-09-20, the Dynamic Type row); the tab now reads "Map", which is what the same place is called on the web, while the screen's own heading stays `gw.title`. The iPhone still has no street map behind it. | Four tabs should be named the same on both clients, and a person told about the Map tab should find something called Map. Naming the tab after its one current screen made the two apps disagree about their own shape. |
| 2026-09-20 | Accepted | **SEMCOG's required copyright notice is printed on the Safe streets panel** (`apps/web/src/hoods.ts`, `SEMCOG_NOTICE`, marked `lang="en"` so it is never translated), which closes the condition of use the Copyright License Agreement sets. **Known limit:** it is a constant in the app, with the year written into it, while the bundle carries the same sentence with the year derived from the layer's own last-edited date (`sources.crashes.notice`). The app does not read it. Whoever next refreshes the crash layer should either read the bundle's copy or check the constant. | A required notice that is only in NOTICE is not on the screen where the data is used. Printing it was the point; the hardcoded year is a smaller wrong than not printing it, and it is written down so it is not discovered by a lawyer. |
| 2026-09-20 | Accepted | **Before the list has loaded, every listing id is treated as private.** On a cold load the router starts before the bundle is read, so it could not know that `#/r/<id>` named a DV or crisis listing and left it in the address bar. `sensitiveId` now fails closed (`apps/web/src/main.ts`), and `router.retrace()` gives a public listing its address back the moment the list says it is public. Tests drive both cases (`apps/web/test/router.test.ts`). | Found by the final docs pass after the adversarial review: the rule was right and the moment it ran was wrong. Failing closed costs a public link a few hundred milliseconds without its address; failing open costs a person their privacy. |
| 2026-09-20 | Accepted, on Kyle's word; the written record is Open | **The City had the six overdose steps approved, and the screen names the Detroit Health Department.** Kyle, 2026-09-20: the six steps in the app (`od.s1` to `od.s6`, unchanged since 2026-09-18) are the ones the City had approved. `od.review_note` now reads "Call 911 first. The Detroit Health Department approved these steps." in all four languages (the Spanish, Arabic and Bengali are machine drafts, in the native-review queue). A test pins the six steps word for word, so they cannot drift away from what was approved without someone noticing, and pins that the line names no other source and promises nothing about the law. **Still owed, for Kyle:** the record of the approval (who, when, which document or message) in this repository; until then the sentence on a life-critical screen rests on one person's account, the same footing as the DHD row above. If the steps are ever edited, the sentence comes off until the new wording is approved. | Kyle's decision. It replaces, the same day, "No doctor has checked these steps yet", which the adversarial review had put there because the earlier "follows national public health guidance" named no source. |
| 2026-09-20 | Accepted | **Kyle's five answers after the adversarial review.** (1) **The DHD conversation stays in the docs and nowhere else**: no sentence about DHD staff's informal support in the README, NOTICE, the app or a store listing; in `docs/` it stays marked as Kyle's account. (2) **`kyle@linwoodtechnologies.com` is the address the app shows** (About, on web and iPhone; NOTICE). Kyle's personal address still stands in his own signed `api/edge-protections.md`, which no agent edits; changing it there is his. (3) **Children's Hospital of Michigan's emergency room carries 313-745-5437**, the keypad digits of the "(313) 745-KIDS" its own page prints, entered on the steward's instruction; the label and the notice both show the letters, so a resident can see where the digits came from. This is the same mapping `page-match.ts` uses when it reads a page. (4) **The About screen says whose app this is not**, second paragraph, all four languages (`about.independent`): "This is an independent project. It is not from the City of Detroit, DDOT, SMART or the Health Department." Built on web, iPhone and Android; the Android About screen also stopped printing `{version}` and `{date}` as raw text. (5) **SEMCOG's Copyright License Agreement is accepted for now, indemnification clause included**, with its terms correctly described this time; the required notice is printed on the panel. Asking the Michigan State Police about the records underneath stays Open for after the hackathon submission. | Kyle, 2026-09-20. Each answer closes an Open row above; the Arabic, Bengali and Spanish of `about.independent` are machine drafts and join the native-review queue. |
| 2026-09-20 | Accepted | **Android reads the bundle's JSON as UTF-8 bytes, and only parses the files a screen uses.** `Json.parse(ByteArray)` replaces the character-at-a-time reader (same `Json` tree, same depth and size caps, no dependency); the old reader is kept in `query/src/test` and `JsonParityTest` holds the two to the same answer on every fixture and every bundle file. `BundleStore.load` still checksums every file the index lists but no longer parses `places/parks.json`, `places/transit.json` or `places/zips.json`, which no Android screen reads. Loading categories on demand and parsing in parallel were measured against and not done. Numbers in `apps/android/README.md`, "JSON, measured". | Measured first: most of `load.json_parse` on the emulator was the loader thread waiting for a core, and of the real work about two thirds went with the `String` copy, the per-string `StringBuilder` and re-decoded keys. A malformed unused file with a matching checksum can only come from the publisher, where schema validation catches it. |
| 2026-09-20 | Accepted | **A domestic-violence shelter sorts by proximity without publishing anything that locates it.** Kyle's ask was both halves at once: never publish a DV shelter's address, and still let people see which shelters are near, because they must call. So a `shelter.dv` row (and any sub-category) may never carry `address_1`, `zip`, `lat`, `lon`, a `location_name` that is a building or a street, or a website/source URL whose path is an address page — **not even when the shelter prints its address on its own page**, which overrides the ordinary "a shelter's address is shown only if the shelter publishes it" rule for this one category. It is a hard build error in `pipeline/src/validate.ts`, and `validateHsdsPrivacy` checks the published HSDS export on its own terms too (`virtual`, no addresses, no coordinates). Instead a steward may record `service_area`: one id from a **closed list of coarse areas** (`detroit`, `dearborn`, `hamtramck`, `highland_park`, `wayne_county`, `wayne_county_west`, `wayne_county_downriver`, `statewide`, `national`), a whole city or bigger, never a ZIP and never a neighbourhood. Each area maps **in code** (`packages/query/src/areas.ts`, mirrored in Swift and Kotlin, parity-tested) to one fixed public reference point: a city hall, or a county's geographic centre. With a location shared, a DV row bands by the distance from the person to that point in coarse bands (0–3, 3–10, over 10 miles); `statewide` and `national` rank after every local area; a row with no area ranks as it does today. The ranked result's `miles` is always null, so no client can show a distance, a map, a dot, directions or a bus link; the screen shows the area in words ("Serves Detroit"), a Call button, and "For safety, this shelter does not share its address. Call and they will tell you where to go." Spec in `schema/query-spec.md`, fixtures in `schema/fixtures/13-dv-service-area.json` (9 cases, passing in TypeScript, Swift and Kotlin). A DV row also publishes **on its phone alone** in `check:sources`, because it may never carry the street number that rule otherwise asks for (docs/04). | Every input to the order is public and per-area — the person's own location, which never leaves the device, and a city hall's coordinate that is the same for every shelter in that area — so the order can say which area a row serves, which the screen says in words anyway, and nothing finer. Two shelters in one area always tie on the band and are separated only by their open key and their ids, neither of which is location-derived. The alternative on the table, keeping DV rows unsorted, meant a person in Dearborn reading a Detroit line first in a moment when the nearest line is the one that matters. |
| 2026-09-21 | Accepted | **The four open accessibility items are closed; this supersedes the 2026-09-20 row that accepted the two map-contrast ones.** (1) **Minor streets and the ground outside the four cities reach 3:1 without a grey slab.** Every road class is now measured against the land, a park *and* the out-of-area ground, in both themes and again with `prefers-contrast: more`: minor streets 4.01:1 / 4.22:1 on land and 3.07:1 over a park. The map stays readable because the colour was only one of four changes — a small street is a 0.9 px hairline, it appears at 6 m/px instead of 9 (and the next class at 11 instead of 16), width carries the hierarchy instead of fade, and close in the big roads get a casing. The ground outside the cities is **not a fill at all** any more: no colour can be 3:1 against the land without being darker than the streets, so it is sparse diagonal **hatching** in a new `--map-out-ink` (3.31:1 over its own ground, 3.86:1 against the land). Routes, dots and the map's focus ring are measured against the casing that is actually under them, because no single colour can clear 3:1 against both a near-white land and a 3:1 street. `prefers-contrast: more` and `forced-colors: active` are answered **through the map tokens**, since a hand-painted canvas gets no system repaint; a test enumerates every token `map.ts` reads and fails if one is missing from the forced-colours block. (2) **The map is operable, not only describable.** With the picture focused, N and P walk the features on screen — the greenway in route order, then places nearest the middle — Enter opens one, Escape steps back out; each is announced through the map's own live region with the card a tap would give. N and P rather than Tab, because Tab must keep leaving the map (2.1.2) and the arrows must keep panning it (2.5.7); a bare letter is 2.1.4's own exception for a control that has focus. The ring can only land on a row `mapDrawable` allows, and a dot with no category is not walked: it fails closed. (3) **A traceless screen is titled by its PURPOSE**: "Find help", "Search", "Saved", "Listing", "Urgent help", each followed by the app name. Every one is equally true of somebody looking for a food pantry, so 2.4.2 is met and docs/08's rule — that a title must never *give away* what a person needs — is untouched; the specific name stays in the `<h1>` and is also said through the live region. (4) What only a person can settle is **a roadmap item, not an open failure**, and now has a script: `docs/ACCESSIBILITY-TEST-SCRIPT.md`. | The two contrast items were accepted on 2026-09-20 with the honest note "Kyle's call", because the only fix anyone had was a uniform darkening that would have buried the greenway, the layers and the dots. The fix was never a colour on its own: it is colour plus line weight plus the zoom a thing first appears at plus a texture for an area, and once those are separated each one is cheap. The same is true of the keyboard: "there is a list" was a real equivalent and still is, but it was being used as a reason for the picture to answer nothing, and two letters were enough. |
| 2026-09-21 | Accepted | **Language is chosen from a native `<select>` in the top bar, not a row of links under it.** One compact pill — a globe, and the language in use written in its own name — sits between the app's name (or the page title) and Urgent help, on the four tabs and About. A real select and not a button-and-menu of ours: on a cheap Android phone, and under Switch Control, VoiceOver and TalkBack, the operating system draws the list, it takes a single switch action, and none of the menu-button pattern (`aria-expanded`, arrow keys, Escape, click-outside, focus return) is ours to get wrong. Every option carries its own `lang`; the control's name is an `aria-label`, because a `<label>` that *wraps* a select takes the select's own options into the computed name ("Language English Español العربية বাংলা" — found live). One line at 320, 375 and 1280 px in all four languages including right-to-left. **Below 30 rem the app's mark is hidden from the eye and kept for a screen reader**: three things do not fit on a 320 px line, Urgent help may not give way, and the name is the piece that can. On a pushed screen the control is not drawn at all — the bar there is already a back button, an owner-written name and either Urgent help or the quick exit — and the tabs are one tap from every screen. It is not cloned into the full-screen map's bar. | Kyle, 2026-09-21: "merge the language selection and header into one line." A row of four links was a second line of chrome above every screen a person browses from, and the language they are looking for was the one thing on it that was not a button. |
| 2026-09-21 | Accepted (data and spec; no client draws it yet) | **Two map styles for the transport layers: `standard` (today's drawing, the default on all three clients) and `subway` (an option), specified in `docs/MAP-STYLE.md`.** What the subway style needs — which route each line is (`rt_` ids, a new prefix), short and long names, a palette tone, the owner's `route_color` where a feed publishes one, `frequent`/`headway` only from the owner's data (DDOT's layer: weekday base frequency ≤ 15 min; SMART: its own FAST names), stops in travel order (GTFS `stop_times` for SMART and the People Mover; for DDOT, whose City layers carry no order, by distance along the line within 60 m), shared-street runs and side-by-side slots (≤ 4 routes; more is a trunk), interchanges (75 m), terminals, the People Mover as a closed loop in travel order, the QLINE drawn through its stations and marked `derived`, and cross-system hubs (150 m) — is worked out in the pipeline (`pipeline/src/transit-network.ts`) and travels in a **second, lazily loaded, signed file beside each network layer, `map/transit/<id>.net.json`**. The standard layer files did not change by a byte, so `standard` draws exactly as before and pays nothing; gz sizes: DDOT routes 42 KB, SMART routes 59 KB, stops 5 KB and 4 KB, QLINE and People Mover under 1 KB; budget 64 KB a file, held by a test. Route colours come from a fixed contrast-checked palette assigned in the pipeline, not from agency colours: every SMART `route_color` fails 3:1 in one theme. | Kyle, 2026-09-21: make each transport layer look like a subway map on all three versions — then, the same day: as an option, not a replacement. Folding the fields into the existing files was tried first and dropped: it quadrupled the DDOT routes file for everyone and changed what the standard style drew. Open for Kyle: brand colours where they pass, the DDOT `frequent` rule, shipping the derived QLINE line (MAP-STYLE.md section 13). |
| 2026-09-21 | Accepted | **The subway map style is settled after two implementations (web, iPhone), so Android builds one spec; every item is marked "(settled)" in `docs/MAP-STYLE.md`.** Badges claim room round-robin (the selected route, then every route's first badge before any route's second; rail › trunks › frequent › the rest) and the 24 are counted as placed. The keyboard / VoiceOver order adds at most 40, of which at most 8 terminals and 20 interchanges, so routes are always reached downtown. Bus tones are handed out across DDOT and SMART together from the committed `.net.json` files (`pnpm ingest:transit --derive`, no network): no two routes that share a street share a tone (a test holds zero clashes). Every hub carries its own `origin`; clients place hubs from it and match names only for an older bundle. The quiet side-street and freeway tokens moved so every quiet street is 3:1 over the quiet park too. Terminal names are near-only, hub names at every band. The iPhone's `standard` basemap was brought to the web's post-audit drawing (tokens in all four values, hairline weights, 6 / 11 m thresholds, hatch, casings), held by a test that reads `style.css`. Rationale: one picture from one bundle on three clients, and accessibility kept, not traded. |
| 2026-09-21 | Accepted | **The app is live at https://313help.com, and the nightly publish is on.** Pages serves the app and the release-signed bundle, the Worker answers at `/v1/*`, `/admin/` and `/v1/steward/*` sit behind Cloudflare Access, `PUBLISH_ENABLED=true`, and the publish workflow's first good run finished 2026-09-21 03:27 UTC (531 listings, `signing: release`, signature good against the active pinned key — checked from outside the same day). From here a merge to `main` reaches residents at the next publish; `PUBLISH_ENABLED` set to anything but `true` is the off switch. DEPLOY-HANDOFF and OPERATIONS keep their steps as the runbook. | Kyle, 2026-09-21: "It is now fully deployed." Verified rather than taken on trust, because every doc said the opposite the day before. |
| 2026-09-21 | Accepted | **The native apps follow the phone's own language list and have no in-app switch.** iPhone and Android pick the first of the phone's preferred languages that we have words for (en, es, ar, bn), falling back to English; right-to-left layout follows. The web keeps its control in the top bar, because a browser's language is often not the reader's. | The platform's per-app language setting already exists, is where people look, and is one less control on a crowded bar. A shared phone is the web's problem more than a native app's. |
| 2026-09-21 | Accepted | **Kyle's answers to the four questions in MAP-STYLE.md section 13: the defaults stand.** (1) Route colours come from **our own contrast-checked palette**, not agency brand colours. (2) **`frequent` is whatever the owner's data carries** — DDOT's weekday base frequency of 15 minutes or better, SMART's own FAST names — never our judgement and never a steward's list. (3) **The QLINE line ships, marked `derived`**, drawn through its stations until a public track source exists. (4) **The text list is unchanged**: identical in both styles; stops in travel order in the list is a separate roadmap item. `subway` stays an option and `standard` stays the default on every client. | Kyle, 2026-09-21. Each default is the choice that keeps a fact a fact: a colour that passes contrast, a word the owner used, a drawing labelled as a drawing, and a list that does not depend on the picture. |
| 2026-09-21 | Accepted | **No address of any kind is kept for a domestic-violence organization, anywhere in the repository — and the National Domestic Violence Hotline is `service_area: national`.** The standing rule covers shelter, office, outreach and mailing addresses, in seed data, `internal_note`, research notes, the bundle and the HSDS export. The YWCA's administrative office address was removed from `docs/research/2026-09-19-new-help/assault-tax-benefits.md` the same day (it remains in git history; it is an office the organization publishes, not a shelter). `national` ranks after every local area, so the hotline is never presented as a local line. CLAUDE.md and docs/08 carry the rule. | Kyle, 2026-09-21. An office address is not dangerous in itself, but a rule with an exception needs someone to judge each case, and the cost of judging wrong here is a person's safety. "None, ever" needs no judgement. |
| 2026-09-21 | Accepted | **The README is a showcase.** About 160 lines; every claim checkable in the repo or on the live site; numbers re-derived from a build. Anything still to do appears **only as a forward-looking roadmap feature** — no test-environment trivia (which simulator, which emulator), no lists of internal shortcomings. Caveats and working lists live where the people who need them look: docs/04, docs/08, CHECKS, the Open rows here, and the platform READMEs. The roadmap is general features, grouped Now / Next / Later, led by **provider-verified listings** as the biggest freshness lever, plus the path from Detroit to Michigan to national (docs/09, docs/12). Nothing untrue may be written to make it read better. | Kyle, 2026-09-21: "The readme should showcase how well we have produced the app; we need to resolve shortcomings or put them in the roadmap... This is way too verbose overall. Compact!" The 500-line README was honest and unreadable; a front page nobody finishes informs nobody. |
| 2026-09-21 | Accepted | **The Android Map tab is built, as the mirror of the iPhone one** (`apps/android/.../Map*.kt`): the city drawn on the device from the signed bundle, the greenway by phase, our own listings by group, the eleven transport layers, a layer switcher kept on the phone only, a full text list, a card on tap, virtual TalkBack nodes through a platform `AccessibilityNodeProvider` (no AndroidX), and a hardware-keyboard walk with the same keys as the web. Views and `android.graphics.Canvas`, no Compose, no libraries. The map's whole arithmetic and `mapDrawable` live in `:core`, ported case for case from `apps/ios/Tests/HelpCoreTests/MapTests.swift`, so CI runs them on a plain JDK. | Third client for docs/05's Map tab; the one rule that must never be got wrong — a sensitive listing is never a dot — is now held to the same fixture in all three apps. |
| 2026-09-21 | Accepted (the iPhone caught up the same day: its standard basemap now parses the web's `style.css` in a test, so the drift this row describes is closed) | **Android takes the web map's 2026-09-21 street ladder and colours, not the iPhone's older pair.** Hairline minor streets, small streets from 6 m/dp (not 9) and the next class from 11 (not 16), `--map-road` `#747d77`/`#75827a`, `--map-main` `#666f69`/`#88968d`, `--map-fwy` `#926c16`/`#a3863a`, the out-of-area ground as a hatch in `--map-out-ink`, and a `--gw-case` casing under every transport line. **Open, for the iPhone:** `apps/ios/HelpApp/MapPalette.swift` and `MapLayers.swift` still carry the pre-audit values, so the two apps draw the same shapes in different greys. | The colours and the widths are one decision (a 3:1 street at the old width is a grey slab). The ported tests pass either way, so the drift is written down rather than hidden. |
| 2026-09-21 | Accepted | **The map travels inside the Android APK.** `map/base.json`, `map/streets.json` and the eleven `map/transit/*.json` layers join the bundle snapshot; `indicators/` and the `.net.json` subway files stay out. A clean debug APK goes from 1,282,907 to 1,675,641 bytes (1.22 → 1.60 MiB). Every file is still checked against the sha256 in the signed index before a byte is decoded, wherever it came from. | docs/05 promises a map that works with no signal *ever*; one that needs a network the first time does not. 380 KB is the price. |
| 2026-09-21 | Accepted | **The Android map answers the gestures a phone map is expected to answer** (Kyle, 2026-09-21: "have the map utilize pinch to zoom and other common gestures on mobile"): drag with a flick that settles, pinch that zooms about the fingers **and pans with them**, double tap to zoom in, double-tap-and-drag to zoom with one thumb, a two-finger tap to zoom out, and a tap that still selects. `GestureDetector` and `ScaleGestureDetector` only. **No rotation and no tilt**, on purpose: north stays up, so the city on the screen matches the city outside, street names stay the right way up, and the N/P walk and the TalkBack node order stay the plain south-to-north order. Nothing animates when the phone is set to do without motion. | Kyle. The composition arithmetic (`MapCamera.pinched`, `MapFling`, `mapZoomStep`) is in `:core` with tests, because a gesture that is only checkable by hand is a gesture nobody checks. |
| 2026-09-21 | Accepted | **Android's Map tab is a fifth tab, and the tab bar's words are 12 sp.** Home · Help · Map · Search · Saved places. At the old 17 sp with 16 dp of padding, "Saved places" broke in the middle of a word. Nothing is ellipsized and the words still grow with the phone's text size. | A fifth tab has to fit on a phone; the alternative was an abbreviation nobody asked for. |
| 2026-09-21 | Accepted | **Phone-map gestures are the same on all three clients, and the map never rotates or tilts.** Pinch zooms about the fingers and the map follows them (two-finger pan mid-pinch); one-finger drag with fling, stopped by the next touch and by the camera's own limits; double tap zooms in about the tap; double-tap-and-drag zooms with one finger; a two-finger tap zooms out; a tap still selects. No momentum or animation under reduced motion (web `prefers-reduced-motion`, iOS Reduce Motion, Android animator scale 0). On the web a touch tap waits out the 300 ms double-tap window before selecting (a mouse click selects at once), and the small inline maps on listing pages take two fingers to pan so a thumb scrolling the page is never trapped. The maths is pure and tested on each client (`gestures.test.ts`, `MapTests.swift`, `MapTest.kt`). | Kyle, 2026-09-21: "pinch to zoom and other common gestures on mobile". North stays up so the keyboard walk and the screen-reader order keep meaning something. Multi-touch could be driven for real only in the iPhone simulator; on Android and the web it rests on the tests. |
| 2026-09-21 | Accepted | **The Subway style on Android, with three stated differences from the spec.** (1) Solid route strokes are drawn with `Canvas.drawLines` on segments flattened from the cached per-route path (at most 3 chords a corner, round caps for joins) because stroked `Path`s were about three times slower on the emulator; dashes still use the path. (2) "More contrast" is read from `UiModeManager.getContrast()` (API 34+); below that there is no public API at minSdk 24, so the plain palette, which already clears 3:1, is used. (3) A DDOT route card carries no trip-planner link, because DDOT's planner is `http://` and the app opens only `https://`. The palette is held to the web's `style.css` by a test that parses it (72 subway values, 36 standard). | The style is optional and Standard is the default; frame time in Subway on the emulator is about twice Standard's and has not been measured on a phone, which the roadmap's device testing covers. |
| 2026-09-21 | Accepted | **The Map tab asks for location on its first open, behind our own card, and then shows a two-mile radius.** The first time the tab is opened on a phone it draws a small dismissible card over the map — "See what is near you?" / "Your location stays on this phone. We never send it or save it." / **Use my location** · **Not now** — and only that button asks the platform. **Coarse only** on all three clients (`enableHighAccuracy:false`, hundred-metre accuracy with no full-accuracy request, `ACCESS_COARSE_LOCATION`). A fix inside the four cities animates the camera so the **shorter screen dimension spans four miles** (a two-mile radius), clamped to the existing zoom and pan limits; a fix outside them moves nothing and says so, with the ZIP entry beside it. A refusal or timeout leaves the map as it was and nothing re-prompts on its own. **The only thing stored is a boolean** — that the card was answered — in the same per-device store as the layer choices; the position is never written anywhere, including the camera the map remembers, which is memory only on every client. The decision (`firstOpenAction`) and the camera maths (`cameraForRadius` / `MapCamera.forRadius`) are pure functions with the same table of cases on web, iPhone and Android. | Kyle, 2026-09-21: "prompt the user to allow using location when opening the map for the first time, then zoom into a 2 mile radius." A cold permission prompt explains nothing and gets refused once and for ever; a browser penalises a prompt with no gesture behind it. Two miles is the walk-and-bus distance the rest of the app already sorts by, and coarse is all a dot on a city map needs. Storing anything more than the flag would break the promise in docs/08 for a convenience nobody asked for. |
| 2026-09-22 | Accepted | **One row per service; a category says what the row offers, not who runs it or where it is; and a map layer's label names everything in the layer** (category audit, `docs/CATEGORY-AUDIT-2026-09-22.md`; Kyle: "why is the main library categorized under clothes, showers and things?"). A row carries exactly one category. A door that truly offers two kinds of help gets two rows under one `org_id` at one address (HSDS: one organization, one location, several services), the way Pope Francis Center already has a meal row, a shower row and a day-center row; a second row comes in like any other (`data/seed/incoming/` → proposed → the own-page check), never by copying. Rows of one family share a category: a library building is `rec.library`, a Health Department Neighborhood Wellness Center is `health.dhd`, help signing up for benefits is `money.benefits`, a property-tax hardship exemption is `housing.owner`, recovery coaching is `treatment.recovery`. Eight rows were re-filed with dated `internal_note`s; nothing was deleted and no id changed. The Map tab has **eight** help layers on all three clients: `connect` moved in with `rec` ("Libraries, rec centers, and internet"), `kids` + `youth` became "Kids and teens", and what is left of the old catch-all is "Clothes, showers, and pets"; "Places to sleep" became "Shelters and day centers" and the last layer now says "and rides". `KNOWN_CATEGORIES` in `pipeline/src/validate.ts` is the one list of the 46 slugs, and a web test holds the need screens, browse chips and map layers to it. | The Main Library was the one library filed under `connect`, and `connect` sat in a layer called "Clothes, showers, and things" with kids, youth and pets: an inconsistent row inside a catch-all whose name did not describe it. A label that lists what is in the layer cannot surprise anyone. es/ar/bn labels are machine drafts for native review. |
| 2026-09-22 | Accepted | **"I want free Narcan" lists every place that stocks naloxone, and a daytime clubhouse is no longer hidden like a crisis line** (Kyle's two answers to the category audit's judgement calls, K1 and K3: *"add all the wayne county narcan stations and get the goodwill clubhouse out of the hidden address directory"*). **K1:** the Narcan need now queries the whole `harm` top-level on web, iPhone and Android — `harm.narcan` (the Health Department's boxes) plus `harm.supplies` (Wayne County's 26 Well Wayne stations in Hamtramck, Highland Park, Dearborn and Detroit, and the Life Points outreach), every one of which says it gives out naloxone. Ranking is unchanged (open now, then distance) and Home's shortcut opens the same screen; the neighbourhood panel's "nearest free Narcan" was moved to the same query so it cannot name a different place. `sal_dearborn_department_free_narcan_and_test_strips_at_the_w` was re-filed `harm.narcan` → `harm.supplies` with a dated note, which drops nothing because of the first half; whether it is the same box as `sal_wws_dearborn_wagner_place_parking_garage` is a steward item (`docs/CHECKS-2026-09-20.md` § 1.17), to be archived through the queue if it is, never deleted. **K3:** a new **non-sensitive** category `health.support` ("Mental health support") for ongoing mental-health support that is not a crisis service, and `sal_goodwill_industries_a_place_of_our_own_clubhouse` moved into it with a dated note after Goodwill's own locations page was re-read on 2026-09-21 and still prints "1401 Ash Street, Detroit, MI 48208" and 313.931.0901. It is reachable from "I need a doctor, dentist, or eye care" → *Mental health support*, and from "I need to talk to someone" under its own heading **below** 988, the DWIHN line and the crisis places (docs/05 ordering); that screen keeps its quick exit and leaves no URL, while a `health.support` listing is an ordinary listing with an address, a map dot, directions, Save, Share and its own URL. The sensitive set is untouched — still exactly `shelter.dv` and `health.mental`, matched whole or as a parent — and tests on all three platforms pin both halves. `KNOWN_CATEGORIES` is now 47 slugs. | Twenty-six stations that all stock Narcan were invisible on the one screen asking for Narcan, and the one row that carried test strips was the only one on it: half the answer, twice. A clubhouse for adults living with serious mental illness was being treated as a crisis line, so the app withheld an address its owner publishes on its own website — protection that helped nobody and stopped people getting there. Splitting the slug was cheaper and safer than loosening what "sensitive" means. |
| 2026-09-22 | Accepted | **An ingested id belongs to one record of the publisher's, for good.** An id for a row read from an open data source is keyed to that publisher's permanent record reference (`GlobalID` on the City's ArcGIS layers) and never to the order the service answers in: a known reference keeps its id even when its name or address text changes, only a never-seen reference is minted a new one (in reference order — address slug, then name slug, then a short hash of the reference), and an id whose record has vanished is written to a committed `data/ingested/<source>.retired-ids.csv` and **never reused** — though that same record gets its own id back if it returns. `pnpm build:bundle` **fails** if one id stands for two records or a retired id turns up on a different one, and the ingest **warns** "two records at one address in one category" so likely duplicates in an agency's layer reach a person in the nightly pull request. Two sources publish no reference — Wayne County's Google My Map (key: the city and site name it states) and the City's Recreation Centers layer (key: the site name) — so a rename there reads as a new record; that limit is written down in `pipeline/src/ingest-ids.ts`. | On 2026-09-21 the City's Narcan layer returned its two boxes at 13601 W. McNichols in the other order and `sal_hr_13601_w_mcnichols` silently moved from the Citgo record to the Amoco one. Reports, saved places and steward decisions all hang on that id, so an id that can move is a data-integrity bug, not a cosmetic one. |
| 2026-09-22 | Accepted | **Neighborhoods is a tab, at the address it already had.** Kyle, 2026-09-22: "I don't know why all the neighborhood info is buried in the about page, it should have its own tab." The tab bar is Home · Help · Map · Neighborhoods · Events, of which a phone normally shows four because Events still hides itself when the bundle carries none. The tab's hash is the `#/n` the screen has had since it was built, so every link, bookmark and Back button keeps working; `{ v: 'hoods' }` is now only the greenway lens. Its screen adds three things the About link never had: **Your neighborhood**, found on the device by point-in-polygon against the outlines the bundle already carries (`apps/web/src/hoodfind.ts`, cases in `schema/neighborhoods/points.json`) — nothing is sent, nothing is stored, and outside Detroit it says so rather than naming the nearest; a search over the 205 names; and the list in A–Z or council-district order, never in an order a number could set (docs/13, rule 1 — an index row carries no indicator at all, and a test holds it there). On the phone bar the label is a short word ("Areas"; "Barrios", "أحياء", "এলাকা"), because at 320 px with five tabs a label has 55 px and "Neighborhoods" needs about 85; the short word is the whole of that button's name, not a truncation and not a different name from the one a screen reader reads (WCAG 2.5.3), and the side rail, the window title and the screen's own heading carry the full word. The iPhone and Android apps have no neighborhood pages, so they do not get the tab. | The pages answer the questions a block club actually asks, and they were one line down a screen almost nobody opens. Keeping `#/n` means the change costs no existing link. The label is the only string in the app chosen by measurement, and it is measured rather than guessed because a word broken across two lines at the bottom of a cheap phone is not a label. |
| 2026-09-22 | Accepted | **A neighborhood's "nearest free food / clinic / Narcan / place to be indoors" row names that place and opens it.** The numbers file carries a new `help.nearest_id` beside `help.nearest_miles` — the `sal_` id of the very listing each distance was measured to, `null` exactly where the distance is `null` — and the web page draws "Free food — Capuchin Soup Kitchen · 0.6 mi" as a button to that listing, with the whole fact as its accessible name and the owner's words marked `lang="en"`. **Additive:** `nearest_miles` did not change by a digit, so the iPhone and Android apps, and any older bundle, keep working untouched. A pick must have a coordinate and must pass `canBeNearest`, which refuses a sensitive (`shelter.dv`, `health.mental`) or private (`treatment`, `assault`) listing **whatever the four kinds are mapped to** — none of them reaches one today, and the guard is there so a later edit to the mapping cannot quietly put a DV shelter on a public page; the web checks the category again before it draws a link, because the numbers and the listings are two files under one signature. Ties go to the smaller distance, then the lower id, so one bundle always names the same place. The page falls back to today's plain distance row for an older bundle, for an id this bundle no longer carries, and for anything the category check refuses. Cost: `indicators/neighborhoods.json` 66.7 → 75.8 KB gz, downloaded only when a neighborhood screen opens; the app's main JS is unchanged at 85.5 KB gz. | Kyle, 2026-09-22: "we need to fix the gaps." A page that says free food is 0.6 miles away and will not say where is a fact with no door on it. |
| 2026-09-22 | Accepted | **The Map tab opens on a two-and-a-half-mile radius around Detroit City Hall, not on the whole four-city region.** With no location known — before the first-open card is answered, after "Not now", after a refusal, or for a fix outside the four cities — the first view is `cameraForRadius(anchor, 4023.36 m)`: the same arithmetic as the two-mile "centre on me" view, with a different centre, on web, iPhone and Android. The anchor is **not a new number**: it is `SERVICE_AREAS.detroit.point` (42.3293, −83.0452), Detroit City Hall — the Coleman A. Young Municipal Center — which the app already carries in code as the `detroit` service area's reference point, so the three clients cannot drift and nothing new about the city had to be typed in. Measured: the shorter side of the map box spans five miles, which is **23.5 m per pixel** on a 375 px phone (was 66 m per pixel) and **12.6–13.4 m per pixel** on a laptop — the `mid` band of docs/MAP-STYLE.md, where street classes 0–2 are drawn and classes 0–1 are named, so Woodward, Gratiot, Michigan Ave and Grand River are legible and the greenway reads as a path rather than a thread. A location already known — allowed earlier, or the centre of a typed ZIP — still wins and keeps the two-mile view unchanged. **Nothing else changed:** the pan and zoom limits are untouched (two pinches, or one press of −, still reach the whole region), the reset button (⌖) still goes to the four-city fit, and the maps that are about one subject — a listing, a greenway stretch, a park, a neighbourhood outline — keep fitting their subject. `reset()` on iPhone and Android now counts as a move, so the next layout keeps the region the person just asked for instead of returning to the anchor. | Kyle, 2026-09-22: "the initial map presentation needs to be much more zoomed in." The region fit put four cities in a phone-sized box: every street a hairline, no street name but a freeway shield, and the greenway a thread — a map that could not answer "what is around here?" until the person zoomed in twice. Two and a half miles is a little wider than the you-are-here view on purpose: the anchor is the city's front door, not where the person is. |
| 2026-09-22 | Accepted | **A neighborhood's by-year panels can be read as a table or as a chart, and Table is the default.** Kyle: "for the longitudinal data, we should give the user the option on page to switch between a table and chart view for relevant data", and then, having looked at it: lines rather than bars, with homes sold and building permits on the SAME chart and each one selectable. One Table/Chart flag for every panel on the page, kept on the device beside the map layer choices and never sent; a real radio group on all three apps, applying at once, announced, with the focus left on the control. The chart is **one pair of axes and a line per series** — one y-axis, never two — with a key of two real toggles, both on by default, either switchable off, **never both** (the last one is disabled and the panel says why). Each line is told apart by colour, by the shape of its point and by the pattern of its line, so the pair survives grayscale, print and forced colours. Drawn with **no library at all**: inline SVG on the web, Swift Charts on the iPhone, a plain Canvas plus an `AccessibilityNodeProvider` on Android. It shows **counts**; prices and permit costs stay in the table and the chart view says so. Every point carries its own accessible name and its value on hover and on focus; the table stays reachable in every view (visually hidden on the web, kept on screen under the chart when a screen reader is running on the phones). | Kyle, 2026-09-22. A table of eight years is a wall of numbers; a shape is faster to read, and the two series are only worth anything read against each other. The rules in docs/13 decide HOW it is drawn — a picture must not undo what the numbers were published with. |
| 2026-09-22 | Accepted | **Home sales and building permits state their real count, however small; the under-5 rule stays everywhere else.** Kyle: "just use the actual number, there is no need to truncate or round anything." `pipeline/src/ingest-neighborhoods.ts` gains `plainCount` beside `suppress`, and the sales and permits series use it; the ingest was re-run and `data/ingested/city_stats.json` and `data/indicators/neighborhoods.json` rebuilt, so 211 neighborhood-years that read "fewer than 5" now read 1, 2, 3 or 4. **A median price still needs ten sales** — the middle of three moves with any one of them — and the count beside it is now always shown; the panel says "Small numbers change a lot from year to year." in place of the hiding. **Unchanged: everything that is about people or that the City itself suppresses** — crashes (docs/13's identification reason is real there, and the panel stays five-year totals only), blight tickets, demolitions, reported problems, building fires, rental certificates, vacant registrations and rated street pieces. The clients keep the whole `lt5` path, because those series still use it and an older bundle may carry it for any series. | A sale and a permit are public transaction records the City already publishes with the address on them: hiding a 3 protected nobody and made our page say less than the source it cites. The reason for suppression in docs/13 was identification, and it still applies wherever a count is about a person. **CLAUDE.md's "small counts suppressed and shown as words" line now describes only part of the data and needs Kyle's own edit; it was deliberately not changed here.** |
